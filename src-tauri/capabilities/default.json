{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for CTRL windows. Core invoke + window/event/menu/tray APIs and PWA plugins. BYOK keychain access lives in a SEPARATE capability (see keychain.json) so it can be gated independently once WASM keycaps land — they must NOT inherit BYOK reach via this default scope.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:webview:default",
    "core:window:default",
    "core:tray:default",
    "core:menu:default",
    "global-shortcut:default"
  ]
}
