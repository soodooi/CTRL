<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="CTRL.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:CTRL"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   FontSize="28"
                   FontWeight="SemiBold"
                   Text="CTRL — Rust Kernel Bridge" />

        <TextBlock Grid.Row="1"
                   FontSize="13"
                   Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                   Text="WinUI 3 + P/Invoke to ctrl_lib.dll (W2 smoke test)" />

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <Button x:Name="BootButton"
                    Style="{StaticResource AccentButtonStyle}"
                    Click="BootButton_Click"
                    Content="1. Kernel Boot" />
            <Button x:Name="HealthButton"
                    Click="HealthButton_Click"
                    IsEnabled="False"
                    Content="2. Kernel Health" />
        </StackPanel>

        <Border Grid.Row="3"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="8"
                Padding="16">
            <ScrollViewer>
                <TextBlock x:Name="OutputText"
                           FontFamily="Consolas, 'JetBrains Mono', monospace"
                           FontSize="12"
                           IsTextSelectionEnabled="True"
                           TextWrapping="Wrap"
                           Text="Ready. Click 'Kernel Boot' to initialize the Rust core via P/Invoke." />
            </ScrollViewer>
        </Border>
    </Grid>
</Page>
