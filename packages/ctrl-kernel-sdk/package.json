{
  "name": "@ctrl/kernel-sdk",
  "private": true,
  "version": "0.1.0",
  "description": "L2 SDK — TypeScript syscall surface for CTRL L1 Rust Kernel. Exposes Actor, Capability, Event, Channel, Effect primitives.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  },
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  }
}
