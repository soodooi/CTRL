{
  "name": "@ctrl/keycap-sdk",
  "version": "0.1.0",
  "private": true,
  "description": "Standardized keycap interface for CTRL clients (Irisy / PWA / future Janus). Wraps MCP protocol + Tauri invoke + REST so consumers don't care which underlying transport a keycap uses.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
