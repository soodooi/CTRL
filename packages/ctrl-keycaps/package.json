{
  "name": "@ctrl/keycaps",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Source-of-truth for CTRL's bundled builtin keycaps (ADR-024 v2 — currently builtin-assist + builtin-create; user keycaps install into ~/.ctrl/keycaps/). Bundled into CTRL.app/Contents/Resources/keycaps/builtin/ at build time; seeded into ~/.ctrl/keycaps/ on every launch (shell/builtin_keycaps.rs).",
  "scripts": {
    "validate": "echo 'keycap manifest validation lands when ctrl-keycap-sdk schema is finalized'"
  }
}
