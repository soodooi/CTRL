{
  "name": "@ctrl/desktop",
  "private": true,
  "version": "0.1.0",
  "description": "Olym desktop runtime — 5 vendor-agnostic ports + reference adapters for CTRL Tauri host.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./ports/*": "./src/ports/*.ts",
    "./adapters/*": "./src/adapters/*.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@manidala/olym-core": "*"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  },
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  }
}
