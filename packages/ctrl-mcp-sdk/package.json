{
  "name": "@ctrl/mcp-sdk",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Standardized mcp interface for CTRL clients (Irisy / PWA / future Janus). Wraps MCP protocol + Tauri invoke + REST so consumers don't care which underlying transport a mcp uses.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src"],
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
