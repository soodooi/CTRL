{
  "name": "@ctrl/pi-bridge",
  "version": "0.1.0",
  "private": true,
  "license": "UNLICENSED",
  "description": "Pi extension that routes Pi's LLM calls back into the CTRL kernel provider sub-system (ADR-003 / ADR-004 §9.1). Loaded into Pi via `pi --extension <bundled-path>`; registers a `ctrl-bridge` provider whose `streamSimple` POSTs to localhost:$CTRL_PROVIDER_PORT/text-chat.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "files": ["src", "README.md"],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "peerDependencies": {
    "@mariozechner/pi-coding-agent": "^0.x"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3",
    "vitest": "^2.0.0"
  }
}
