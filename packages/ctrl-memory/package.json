{
  "name": "@ctrl/memory",
  "private": true,
  "version": "0.1.0",
  "description": "Event-sourced AI memory for CTRL — client-side append-only log + cursor-based query facade over @ctrl/stss envelopes.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@ctrl/stss": "*"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  },
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  }
}
