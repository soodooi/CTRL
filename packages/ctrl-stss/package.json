{
  "name": "@ctrl/stss",
  "private": true,
  "version": "0.1.0",
  "description": "ST-SS (Spatio-Temporal Semantic Stream) protocol — CTRL profile (cherry-pick from screi v0.5, with CTRL hardware + e-ink extensions).",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "typescript": "^5.6.3",
    "vitest": "^2.1.0"
  },
  "license": "UNLICENSED",
  "engines": {
    "node": ">=20"
  }
}
