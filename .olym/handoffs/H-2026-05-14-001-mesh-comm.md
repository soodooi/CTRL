---
id: H-2026-05-14-001
title: Multi-device mesh communication — design + spike + library evaluation
severity: P0
status: in_progress
foundation_merged_at: 2026-05-14
reporter: bao
assigned_to: hephaestus
lane: lane-E
reassigned_from: athena (2026-05-19 zeus — mesh impl moved to hephaestus per lane re-org; alias athena-mesh kept in lane-ownership.yaml for hook compat)
worktree: D:/code-space/ctrl-h003-mesh
branch: feat/h-003-mesh-comm
touches:
  - .olym/specs/mesh-comm/                  # NEW spec directory
  - packages/ctrl-mesh/                     # NEW workspace package (Rust + TS dual)
  - ctrl-cloud/workers/ctrl-relay/          # NEW worker (in ctrl-cloud repo, sibling)
  - src-tauri/Cargo.toml                    # +webrtc-rs +vodozemac +automerge dependencies
  - src-tauri/src/kernel/                   # mesh integration with event_bus + persistence
related:
  - H-2026-05-13-001                        # PWA pivot (parent context)
parent_adr:
  - ADR-003                                 # parent decision, you implement it
  - ADR-001                                 # preserves 5 primitives + Cell/Op wire
  - ADR-002                                 # supersedes §8 mobile lane, §9 ctrl-relay
project_id: ctrl-v1
category: feature
created: 2026-05-14
updated: 2026-05-14
---

## 你是 athena

你是 CTRL 项目的并行 lane agent, 处理通讯模块这个**非常关键**的工作 (bao 原话 2026-05-14)。zeus 在 main lane 上做其他工作 (P5/P6 manifest + 创作向导, 或 sub-PR f WebView 验证)。你跟 zeus 是并行关系, 不阻塞彼此。

你的 worktree: `D:/code-space/ctrl-h003-mesh` (git worktree, 独立 working tree, 独立分支 `feat/h-003-mesh-comm`, 跟 main repo 共享 `.git`)。

## 现象

ADR-002 §8 锁的"PWA + cloudflared tunnel"是 v1 临时方案。真正多设备 mesh (Win / Mac / iOS / Android / 未来硬件) 需要专门设计。bao 2026-05-14 同意 zeus 提的 hybrid 方案 (ADR-003 §1 表), 让你来实施。

zeus 已经写了:
- `.claude/ADR/003-multi-device-mesh.md` (Proposed, full 14 章节)
- 本 handoff

你不是从 0 开始, 但**有自主权**在实施细节 (library 选择 / API 形状 / 性能取舍) 上提议修改 ADR-003 草案。bao 是最终决策, zeus 是 sanity reviewer。

## 证据

- `.claude/ADR/003-multi-device-mesh.md` — 你的 spec, 14 章节都读一遍
- `.claude/ADR/001-system-architecture.md` — 5 primitives + 5 source 不可逆 lock
- `.claude/ADR/002-pwa-pivot.md` — UI layer; §8 + §9 + §13 跟 mesh 相关
- `src-tauri/src/kernel/event.rs` — Cell/Op wire format, 你的 CRDT 操作 wrap 这个
- `src-tauri/src/kernel/stss_bridge.rs` (在 feat/h-001-e-cleanup 分支) — 当前 WS bridge + token auth + capability shape, 你的 mesh transport 复用这套接口
- `packages/ctrl-stss/` — TS 协议 + reducer + transport (99 tests)
- `packages/ctrl-memory/` — TS 客户端事件 log (sub-PR c 的伪基础, 可能升级到 CRDT)

## 目标 (按优先级 + 不可越界)

### 你的硬约束 (HARD)

1. **不动 `feat/h-001-e-cleanup` 分支任何代码** — zeus 在 merge 它进 main, 你的工作是 main 起的 worktree
2. **不动 `.claude/ADR/001-system-architecture.md` + ADR-002 lock 章节** (§5 primitives / §4 sources / §不做清单)。如需 amend, 写 ADR-003 supplement 或新 ADR
3. **5 primitives 必须保留** — mesh 是 Channel 的 distributed 扩展, 不是新 primitive (ADR-003 §3.2 已经说清了 mesh primitive 跟 kernel primitive 的层次区别)
4. **不上线公开 cloudflared / ngrok tunnel** — 测试期间用本地 ctrl-relay mock + 两台 PC LAN 配对验证, 不公网

### 你的产出 (按 sprint 顺序)

#### Sprint 1 — Library evaluation + spec extension (1-2 day, gate: zeus + bao review)

1. **`.olym/specs/mesh-comm/spec.md` (NEW, Draft v0.1)**: 把 ADR-003 §3 + §6 + §7 展开成实施级 spec
2. **library 决策报告**: 评估并 lock 这些选型 (写进 spec):
   - **WebRTC**: webrtc-rs vs str0m vs alternative — 体积 / 稳定性 / NAT 通过率
   - **Encryption**: vodozemac vs libsignal-rust vs ring + 手写 noise — 审计程度 / API 工效
   - **CRDT**: automerge-rs (lock by ADR-003 §6.2 但你可以 challenge)
   - **mDNS** (Sprint 4 才用, 但选好): mdns-sd vs zeroconf-rs
3. **Wire format**: 在 `kernel::event::Event` 基础上扩展 `MeshChange` envelope (ADR-003 §6.3 草案)
4. **risk-down spike** for 3 个 🔴 risks (ADR-003 §10): libsignal misuse / device key compromise / MITM at pairing
5. **回汇 zeus + bao**: spec + 选型 OK 才进 Sprint 2

#### Sprint 2 — mesh-core (Identity + Peer + libsignal session, ~3 day)

1. **`packages/ctrl-mesh/`** (NEW workspace package): Rust crate + TS twin (跟 ctrl-stss 同模式)
   - `src/identity.rs` — curve25519 device identity + persistence
   - `src/peer.rs` — Peer struct + state machine
   - `src/session.rs` — libsignal X3DH + Double-Ratchet wrapper (vodozemac)
   - `tests/` — pairing flow round-trip (in-memory mock relay)
2. **集成进 `src-tauri/src/kernel/`**: 不破坏现有 stss_bridge; 新加 `kernel::mesh` 模块
3. **回汇 zeus**: API surface + session test 全绿

#### Sprint 3 — mesh-transport (WebRTC + relay fallback, ~3-4 day)

1. **`packages/ctrl-mesh/src/transport/`**:
   - `webrtc.rs` — webrtc-rs wrapper: datachannel + ICE
   - `relay.rs` — TURN-style fallback (encrypted forward through relay)
   - `negotiate.rs` — picks WebRTC first, falls back to relay
2. **ctrl-relay worker** in **separate ctrl-cloud repo** (zeus 协调): signaling endpoint + relay forwarding
   - 注意: ctrl-cloud 是 sibling repo, athena 不直接 commit 进去, 出 patch 让 zeus 协调
   - 临时: 本地 mock relay (Node.js or wrangler dev — wait, **CLAUDE.md 全局禁止 wrangler dev**), 改用 `miniflare` 或 Node http server mock
3. **回汇 zeus**: 双 PC LAN 端到端 ICE 通过率测试

#### Sprint 4 — mesh-sync (Automerge integration, ~2 day)

1. **`packages/ctrl-mesh/src/sync/`**:
   - `document.rs` — Automerge wrapper for the 3 v1.0 documents (mesh.devices / mesh.keycaps / mesh.preferences)
   - `merge.rs` — incoming change → local Automerge apply → emit kernel Op
   - `outgoing.rs` — local kernel Op → Automerge change → fan out to peers
2. **mDNS LAN accelerator (optional, Sprint 4 末)**: v1.0 可以 ship 时没有 mDNS, 不阻塞
3. **回汇 zeus + bao**: 全套端到端 demo (两台 PC 配对 → 一台改设置 → 另一台 ≤5s 同步)

### Sprint 5 — handback to main (1 day)

1. Sub-PR 拆分:
   - **i** — spec + library lock (ADR-003 acceptance + spec/mesh-comm Stable v1.0)
   - **j** — packages/ctrl-mesh skeleton + identity + session
   - **k** — transport (WebRTC + relay client)
   - **l** — sync (Automerge integration + 3 documents)
2. PR 顺序串行 i → j → k → l, base on main (此时 feat/h-001-e-cleanup 应已 merge)
3. 写 handoff result section: 跟 ADR-003 草案差异 + 实测延迟 / 通过率 / bundle 大小 / risk hit list

## 边界 (硬约束)

- **Rust 1.77+ 严格** (跟主 repo 一致). 不引 nightly features
- **全英文代码 + 中文 .md 文档** (CLAUDE.md 规则)
- **无 `--no-verify`**, 无 force push
- **依赖审计**: 每个新 crate 跑 `cargo deny check`; libsignal-style crypto 必须用 audited fork
- **License**: 所有新 package `private: true` + `license: UNLICENSED`
- **测试**: vodozemac session 必须 round-trip 测试; Automerge merge 必须有 conflict 测试; 至少 30 个 unit + 5 个 integration test 才进 sub-PR
- **CI**: 你可以加 cargo test + cargo clippy 步骤到 GitHub Actions, 但**不修改** zeus 设置的 CI gates (P3.9 binary size ≤ 15 MB)
- **WASM bundle**: ctrl-mesh TS 部分编 WASM 时, **目标 ≤ 350 KB gzip** (ADR-003 §7 + §12 SC budget)
- **多机测试**: 你只有一台机, 双机测试用 docker container (or VM) 模拟另一台 peer

## 已知不确定点 (你判断 + 写决策日志)

- vodozemac API 跟你的 Peer state machine 怎么 fit — 可能要 wrapper, 可能 vodozemac 已经够用. 评估时写下来
- WebRTC datachannel reliability mode (`ordered: true, maxRetransmits: null`) vs unordered/lossy — ADR-003 没锁, 你 ST-SS 适用 ordered 估计
- mock relay 怎么搭最快 — Node http + ws lib 30 LOC 应该够. 不要搭 production-grade 占用 sprint 3 工作量
- 你两台机 LAN 测试不可能, 用 docker — 写 `docker-compose.yml` (一个 service = container peer-A, 另一个 = peer-B, 共享 network) 在 worktree 里
- 当前 PWA 还没 mesh integration UI; 你产出的是 `@ctrl/mesh` 包 + Rust 模块, PWA UI 部分**留给 zeus 在 sub-PR f 接** (Settings → Add Device 二维码 + paired devices list)

## 验收 (松)

ADR-003 §12 success criteria 6 条 + 你的产出 (4 个 sub-PR 可 merge 进 main, 总 commit ≤ 30):
- [ ] Sprint 1 spec + library lock 通过 zeus 中期 review
- [ ] Sprint 2 session round-trip test 全绿
- [ ] Sprint 3 双 docker peer ICE 通过 (P2P 或 relay 兜底)
- [ ] Sprint 4 双 peer 同步 ≤ 5s
- [ ] Sprint 5 4 个 sub-PR + handoff result section + zeus signoff
- [ ] PWA bundle delta ≤ 350 KB gzip 实测
- [ ] kernel binary delta ≤ 3 MB 实测 (用 cargo bloat)

## 协调 (跟 zeus)

| 你做的 | zeus 同期做的 | 协调点 |
|---|---|---|
| ADR-003 实施 | sub-PR e merge + 可能开 sub-PR f (PWA UI 完善) 或 P5/P6 | 你 sub-PR i (ADR accept) merge 后, zeus 在 sub-PR f 接 Settings → Add Device UI |
| ctrl-mesh package | tool-manifest spec (P5) | 完全并行, 你的 mesh.keycaps 文档跟 manifest schema 对齐 |
| ctrl-relay worker patch | ctrl-cloud repo 主路径 | 你出 patch + spec, zeus 在 ctrl-cloud merge (你不直接进那个 repo) |
| 风险 / 决策 / blocker | (always available for sync) | 通过本 handoff 的 Discussion section 沟通; 30 min 内 zeus 响应 |

## bao verbal-go

bao 2026-05-14 钦定:
1. "通讯模块是非常关键的模块" — 优先级 P0
2. "开 worktree, 安排 athena 完成" — 并行 lane, 你独立工作
3. "我 (zeus) 继续其他" — 你不阻塞 zeus, zeus 不阻塞你

## Discussion / 备注 / 决策日志

(athena 在工作中往这里写: 选型理由, blocker, ADR-003 改动建议. zeus 周期性 sync 进来看, 不当 micromanage.)
