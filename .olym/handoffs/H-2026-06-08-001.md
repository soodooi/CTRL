---
id: H-2026-06-08-001
title: Remediate P0 blockers from v0.1.188 full review (5 review agents + E2E)
severity: P0
status: done
reporter: zeus
assigned_to: zeus
lane: zeus
touches:
  - .gitignore
  - src-tauri/Cargo.lock
  - packages/ctrl-mesh/Cargo.lock
  - src-tauri/src/kernel/stss_bridge.rs
  - src-tauri/src/kernel/provider/http_endpoint.rs
  - src-tauri/src/shell/credential_vault.rs
  - src-tauri/src/shell/kernel_supervisor.rs
  - src-tauri/src/commands/updater.rs
  - packages/ctrl-web/src/components/viewers/SvgViewer.tsx
  - packages/ctrl-web/src/components/viewers/MermaidViewer.tsx
related: []
project_id: ctrl-v1-hardening
category: bugfix
created: 2026-06-08
updated: 2026-06-08
---

## 现象

对 `main` @ `2fe07b6` (= release v0.1.188) 做了 ECC 5-agent 评审 (Rust / React /
TypeScript / Security / 架构质量) + Playwright E2E + workspace 单测。交叉确认出
一批 P0 安全/治理 blocker，需逐条修复。本 handoff 跟踪整批 remediation。

E2E: 3 passed / 2 skipped。单测: ctrl-stss 84 passed, ctrl-pi-plugin 6 passed,
**ctrl-pi-bridge 5 failed (test rot — register() 已改为纯 Pi extension, 测试停留在
旧 registerProvider 契约)**。

## 证据

P0 blockers (多 agent 确认):
- `.gitignore:10,13` — Cargo.lock 被 ignore 且未提交 (违反 CLAUDE.md 保命线) ← **本提交已修**
- `src-tauri/src/kernel/stss_bridge.rs:103-115` — WS bridge 能力校验 stub 成 `allow_all`,
  任意持 token 客户端可提交任意 Op (OWASP A01)
- `src-tauri/src/kernel/provider/http_endpoint.rs:71` — provider HTTP 端点 17878 零鉴权,
  本机任意进程可调 vault_write / install_mcp / mcp_run
- `src-tauri/src/shell/credential_vault.rs:50-62` — 凭据库密钥仅由 hostname + 硬编码
  "app.ctrl.spike" 派生, 可离线解密所有 BYOK key (OWASP A02)
- `SvgViewer.tsx:59` + `MermaidViewer.tsx:109` — dangerouslySetInnerHTML 未消毒 = WebView RCE
- `kernel_supervisor.rs:98,104` — async task 内 set_var 数据竞争 (Rust 2024 unsafe)
- `credential_vault.rs:65,71,88,114` — Mutex lock().unwrap() 锁中毒级联崩溃

完整报告见会话记录 (HIGH/MEDIUM 另列)。

## 建议

按危险度顺序: WS allow_all 接 CapabilityBroker → 17878 加 bearer → Cargo.lock (已修) →
SVG/Mermaid DOMPurify → 凭据库密钥派生 → Rust 并发/锁安全。每条独立 commit,
带 [H-2026-06-08-001]。

## 验收清单

- [x] Cargo.lock tracking 修复 (`.gitignore` + 两个 Cargo.lock 提交; `cargo metadata --locked` 双绿) — 46a334a
- [x] WS bridge allow_all → deny-by-default Op 白名单 (full CapabilityBroker session model 是后续) — d12605f
- [x] provider HTTP 17878 bearer 鉴权 (CTRL_PROVIDER_TOKEN, fail-closed, 拒跨域) — d12605f
- [x] 凭据库密钥派生加 keychain 随机 machine secret + 旧数据迁移 — d12605f
- [x] SVG/Mermaid DOMPurify + MarkdownViewer 段切分 XSS — e79dcd9
- [x] Rust set_var 竞争 (移到 spawn 前) + Mutex 锁中毒恢复 — d12605f
- [x] HIGH 批量: http.* SSRF / mcp_host 二进制校验 / updater shell 注入 / soul cap / glob 边界 / callRpc+pi_rpc 白名单 / 硬编码路径 / delta seq / readBody O(n²) / 常量时间比较 — d12605f, 70df62f
- [x] ctrl-pi-bridge test rot 更新 (8 passing, 新 Pi-extension 契约) — 70df62f
- [x] MEDIUM: CBOR 帧上限 / waitlist PII / 死代码示例删除 / license / manifest enum — 08932ea, 95f6d8a, 70df62f
- [x] 自测通过 (cargo check 0 / cargo test 143 passed [3 预存 Windows fail, 基线已证同失败] / ctrl-web tsc 0 / workspace 114 / E2E 3 passed)
- [ ] 发现方确认 (status → verified)

## Deferred (需 bao 裁决 / 单列重构, 未在本批修)

- **功能性中文 NL 处理** (`ctrl-web/src/lib/irisy-reflection.ts` CORRECTION_MARKERS_ZH, `irisy-prompts.ts` 中文 prompt 示例): 违反零中文规则但是功能必需 — 需 bao 定 (a) CLAUDE.md 开例外 or (b) 外置 i18n 资源
- **中文注释 sweep** (~10 文件 .rs/.css 引用 bao 原话): 低风险机械翻译, 留 cleanup pass
- **800 行大文件拆分** (mcp_server.rs 1266 / settings.tsx 1264 / IrisyChat.tsx 1237 / registry.rs 1136): 重构非修复, 单列
- **async Tauri vault 命令同步 std::fs 阻塞** (rust H2): 性能项, 需 spawn_blocking 迁移, 单列
- **full CapabilityBroker session model for WS clients**: 当前是 deny-by-default 白名单 (已闭合 any-Op 洞), 完整 per-session capability 授权模型后续

## 讨论 / 备注

逐条修复, 用户定优先级。从 Cargo.lock 起步 (最快、纯治理)。全量批次 (P0+HIGH+test rot+部分 MEDIUM) 已完成并验证, 6 commits 全挂本 handoff。status → done, 待 bao verify。
