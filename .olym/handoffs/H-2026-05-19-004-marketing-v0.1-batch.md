---
id: H-2026-05-19-004
owner: Apollo (marketing / brand)
status: backlog
created: 2026-05-19
reviewer: bao
parent: H-2026-05-17-apollo-marketing-v0
depends-on: PR for H-2026-05-17 merged + CF Pages preview live
---

# H-2026-05-19-004 — Marketing v0.1 batch (post-launch polish)

Follow-up to [H-2026-05-17-apollo-marketing-v0](./H-2026-05-17-apollo-marketing-v0.md). All items
were intentionally scoped out of v0 to keep the first ship tight (4.7 KB gzip, 5 sections only).
This handoff bundles them into one v0.1 batch — do not split across multiple PRs.

## Scope (single PR)

| # | Item | Notes |
|---|---|---|
| 1 | OG card | 1200×630 PNG, brand cobalt + wordmark; reference `doc/visual-identity/logo.svg`. Place at `public/og.png`. Wire into `<meta property="og:image">` in `Base.astro`. |
| 2 | `sitemap.xml` | Use `@astrojs/sitemap` integration (config in `astro.config.mjs`). Single route for now (`/`), but plumbing ready for `/privacy`, `/changelog`, etc. |
| 3 | `privacy.html` | New Astro page `src/pages/privacy.astro`. One paragraph: "We collect the email you submit to the waitlist. We will use it only to notify you when CTRL opens. We will not sell or share it." Link from footer. |
| 4 | `robots.txt` sitemap line | Update `public/robots.txt` once sitemap URL is final (already pre-wired to `/sitemap-index.xml`). |
| 5 | **themis HIGH-1 fix**: explicit `OPTIONS` handler in `functions/api/waitlist.ts` | v0 carry-over (bao-waived 2026-05-21). Replace `onRequest` 405 catch-all with `onRequestOptions` returning 204 + appropriate CORS headers; keep 405 only for non-POST, non-OPTIONS methods. |
| 6 | **themis HIGH-2 fix**: replace `console.log` PII with persistent sink | v0 carry-over (bao-waived 2026-05-21). Options for bao: (a) CF KV with TTL + at-rest encryption; (b) D1 binding for full waitlist table; (c) Tail Worker → external (e.g. Resend Audience). Pick before real traffic. |

## Out of scope (defer further)

These were also in the v0 deferred list but stay backlog past v0.1 — separate handoff each:

- **Inter Variable woff2 self-host** (~80 KB subset) — only if v0.1 Lighthouse perf score requires it; we are currently at 4.7 KB first paint with system fonts, so the trade-off is uncertain. Quantify before adopting.
- **Waitlist persistence binding** (Resend Audience vs CF KV) — needs bao decision. v0 logs only; Zeus owns binding selection.
- **Chinese bilingual layout** — pending bao copy + ADR-006 product-positioning resolution.
- **HTML → video hero demo** — bao project, separate timeline.
- **Lighthouse audit CI gate** — set up after v0.1 ships, on the actually-deployed preview URL.

## Acceptance

- [ ] OG card renders correctly in Twitter Card Validator + Facebook Sharing Debugger
- [ ] `/sitemap-index.xml` accessible and lists `/` and `/privacy`
- [ ] `/privacy` Astro page renders with same `Base.astro` layout + footer link present
- [ ] First-paint bundle stays under handoff target (<80 KB gzip; v0 baseline 4.7 KB — v0.1 should not regress past ~10 KB even with OG meta + extra page)
- [ ] No new dependencies beyond `@astrojs/sitemap` and `sharp` (sharp only if we generate OG card via script; otherwise hand-author the PNG)

## Branch

`feat/h-2026-05-19-004-marketing-v0.1` (open after v0 PR merges).

## Notes for Apollo (self)

- v0 PR is **PR-pending** at time of writing this. Do not start v0.1 until v0 merges.
- Memory `decision_marketing_stack_astro.md` should reflect "v0 verified 4.7 KB gzip" once Zeus updates it (Zeus stewardship per D-08).
- If bao greenlights weight-claim copy ("the marketing page itself is X KB"), that becomes a separate ADR/copy decision, not part of this handoff.
