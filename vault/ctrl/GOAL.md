# CTRL — 当前开发目标 (single active goal)

> 唯一在跑的目标,锚定所有工作。由 `goal` skill 管理。
> Plain markdown,local 是 truth,bao 拥有这个文件。

## Status: ACTIVE (2026-07-11, bao「开创建功能包的角色 我来开新窗口 你负责项目」)

## 目标 (Goal)

**功能包生产线:专职 pack-builder worker 窗口持续产出新功能包。** 以 `ctrl-stock-cn`
为已验证模板(bao 2026-07-11「你能走通一个就不错了」),按
`share/skills/create-feature-pack` 的强制流程(research → 确认 → manifest → install
→ smoke)一个一个造。zeus 管切片/review/merge;每个包一个切片、一个分支、一个 PR。

- 候选来源:`vault/ctrl/capability-pack-map.md`(Tier 表;ghostfolio 已按 bao 指示
  卸载并从内置出厂里待撤,不做默认候选)
- 锁点不动:manifest=数据 / runtime=通用 / 加 pack 零代码(ADR-002 §7.4)、
  `:17873` gate 治理、secret 不进 LLM、one-shot 原子动作
- 每个包的验收 = create-feature-pack Step 5:装进 `~/.ctrl/mcps/` 且 smoke 真跑通,
  不是「写完 manifest」

其他在册线:remote-window S4 ACL 半成品 park 在 `wip/remote-window-s4-acl`
(tsc 过,行为未验),该线恢复时从此分支起。

## 切片分派 (zeus 唯一写入者)

| # | 切片 | 窗口 | 分支 | 状态 |
|---|---|---|---|---|
| 1 | 第一个新功能包(worker 先调研提案 2-3 个候选,bao 在其窗口拍板后执行) | hephaestus(bao 开窗,名字可改) | feat/pack-\<id\>(定包后建) | assigned |

## 上一目标存档 (2026-07-01 → 2026-07-11, 原 status ACTIVE)

## 目标 (Goal)

**证明功能包真命题 —— 把开源软件变 AI-native:端到端跑通 `ctrl-ghostfolio` 当活体证明。** 复用现成开源 Ghostfolio(自托管财务)+ 其社区 MCP → 经 `:17873` gate 治理(审计+可见性,比它自带的粗 `READ_ONLY_MODE` 开关细)→ §14 化(describe/query/produce)+「AI-native 提升层」(one-shot 原子 / 高信号,不是一 endpoint 一 tool)→ Irisy 能操作它。这一个种子把 create→govern→uplift→distribute 整条功能包链走通,同时打磨 substrate + Irisy 创作流(种子 = 创作流的活体测试 + 护城河示范)。

bao 2026-07-01 校准:「你是不是不知道做功能包的意义 —— 把大量开源软件/MCP 编程成 AI-native 的,这方向你不懂,要调研」→ 深研落 `vault/ctrl/ai-native-feature-pack-research.md`(25/25 主张验证)→ bao 拍「从 ctrl-ghostfolio 端到端跑通」。

**调研定盘(governing)**:砖都有了,CTRL 拼装不重造 —— Agent Skills(SKILL.md)打包 · Anthropic `mcp-builder` 四阶段(含 evals)· AutoMCP OpenAPI→MCP(瓶颈是 spec 质量非 codegen)· 官方 MCP Registry + Smithery(6639)分发 · OpenAI Apps SDK generative UI(structuredContent/content/_meta)· Ghostfolio/Twenty MCP 已有前身 · AWS MCP Gateway = CTRL `:17873` gate 的形状(证明 gate 是行业标准)。**护城河 = ①「raw-wrap → AI-native 提升层」(one-shot/§14/per-call gate 治理,行业普遍欠缺)②「discovery→scaffold→govern→distribute→UI」整条链无统一玩家 = 白地**。CTRL local-first + Irisy + one-shot 哲学正好长在这。

governing = `vault/ctrl/ai-native-feature-pack-research.md` + `vault/ctrl/capability-pack-map.md`(③ 长尾 connector = Irisy 造非 dev 手写)+ ADR-002 §7(composition)+ §7.4(系统化:manifest=数据/runtime=通用/加 pack 零代码)+ §14。锁点不动:5 primitives / 三动词 / `:17873` gate / secret 不进 LLM / plain-text。

## 成功标准 (Success criteria — 可验证)

> 走 dev-loop(编译 + kernel/e2e 测试 + 视觉/独立 checker),绿了 commit。真实 Ghostfolio 连接需 bao 机器(跑着 Ghostfolio 实例 + token);CTRL 侧接线由 in-process 测试 + mock 验证(与所有 gate smoke 同一诚实 gap)。

1. **功能包 manifest**:`ctrl-ghostfolio` manifest(复用 `manifest-schema.ts`)声明连接 Ghostfolio(社区 MCP over HTTP,或其 REST)+ secret(URL+token 从 keychain,**永不进 LLM**)+ capabilities + actions。
2. **连接 + 治理**:Ghostfolio 工具经 `:17873` gate 可达并**记审计 ledger + 受 intent 可见性裁剪**(caller 只在授权时见到);写操作过 review gate。kernel e2e(in-process HTTP 过 gate,mock Ghostfolio 上游)。
3. **AI-native 提升层(护城河)**:把 Ghostfolio 组合暴露为 **§14 RecordSource**(`ghostfolio` describe/query 持仓·交易),Irisy 用统一契约查;+ 至少一个 **one-shot 原子动作**(如「记一笔交易」经 review gate),不是裸 endpoint 镜像。
4. **pack 落地**:装进 `~/.ctrl/mcps/` 被 `loadInstalledPacks` 读到 + `FeaturePackScene` 渲染;Irisy 经 gate 查到组合(真机点或 e2e)。
5. **ADR 对齐**:与实装对齐落 ADR-002 §7 amendment(把「reuse 行业砖 + AI-native 提升层」写实)。

> **第一步(最小可验证)= SC1+SC2 的最小切片**:`ctrl-ghostfolio` manifest + kernel 侧连接一个 mock Ghostfolio MCP 经 gate 可达并记审计(照 Obsidian connector 先例)。

## 候选目标 (next-up — 未激活, 待 bao 拍)

> 不是当前活跃目标。记下来锚定,别丢。

- **LifeOS Notes workspace**(bao 2026-07-01「note 的工作区可以根据 lifeos 来实现」):把 LifeOS 的日历 date-picker + PARA/periodic 树 + 笔记内嵌 §14 查询块**收进 Notes 这一个模块的 workspace**(不是 app 壳)。已起步:`LifeCalendar` 组件 + Task §14 Source 已落地(见进展日志)。设计 = `lifeos-layer-restructure.md`(注:§6.4 已更正「整个 app 变 LifeOS」是脑补;app 壳走 agent-native 那路)。**排在 ctrl-ghostfolio 之后**——功能包命题优先。
- **国内 IM 接入 = 暴露 hermes 已有网关能力**(hermes 上游原生支持微信/企微/钉钉/飞书,CTRL 零接线,只做 wizard + gate 收口)。

## 非目标 / 范围外 (Non-goals)

- **不手写长尾 connector**:③ 层 connector = Irisy 的 mcp-creator 流造(capability-pack-map 铁律);ghostfolio 种子 = **这条创作流的活体测试**,dev 打磨 substrate/流,不亲手写长尾。
- **不重造行业砖**:不自造 manifest/bundle 格式(用 Agent Skills + mcpb)· 不写 codegen(用 AutoMCP 式 + 投 spec-repair)· 不自建 registry(拉官方 + Smithery)。
- **不建整个能力市场**:先一个种子端到端,不铺市场全机制。
- **不 big-bang / 不克隆界面**:功能包的意义是让 agent 原生操作开源工具,不是复刻某 app 的 UI(克隆 LifeOS 前端已判为跑偏)。
- 不动 5 primitives / 三动词 / gate / secret 不进 LLM / plain-text。

## 进展日志 (Progress log — append-only)

- 2026-07-01 **目标替换(LifeOS layer 重构 → 功能包真命题 / ctrl-ghostfolio 端到端)。** 理由:bao 多轮校准把方向拨正 —— 先否「整个 app 变 LifeOS」(那是脑补,LifeOS 只做 Notes workspace),再点破**功能包的意义 = 把开源软件/MCP 变 AI-native**(「这方向你不懂,要调研」)。深研(105 agent/23 源/25 主张 25/25 验证,落 `ai-native-feature-pack-research.md`)结论:砖都有了(Agent Skills/mcp-builder/AutoMCP/MCP Registry+Smithery/Apps SDK/Ghostfolio·Twenty MCP/AWS Gateway=gate 形状),CTRL 拼装不重造;护城河 = AI-native 提升层(one-shot/§14/per-call 治理)+ 端到端整链无统一玩家(白地)。bao 拍「从 ctrl-ghostfolio 端到端跑通」。本 session 副产物(仍有效,归入候选/相关):Task §14 Source + gate e2e(`80ef272/6b71c68/c5dd8e6/e8258b6`,是「把能力变 AI-native」的对活)· Today view + LifeCalendar 组件(`0a33873` + working tree,归 LifeOS-Notes-workspace 候选)· 竞品/参考调研(lifeos-layer-restructure §6.4 + reference memory + 6 repo clone + LifeOS Pro OPC vault)。**下一步 = SC1+SC2 最小切片:ctrl-ghostfolio manifest + kernel 连 mock Ghostfolio MCP 经 gate 可达记审计(照 Obsidian connector 先例)。** 先派 Explore 测绘功能包/mcp_host 机制(动模块先读)。
- 2026-06-20 **目标替换**(原 Irisy 回复正确性测试 → §14 统一操作接口 feature 实装)。理由:bao 连续多轮指挥从「智能表格对标飞书」→「§14 修改架构」→「按架构全量做」,独立 reviewer 复审指出 GOAL.md 旧了。当前状态:`feat/unified-query` 11 commit、kernel 180 测试绿、reviewer PASS、ADR 对齐。下一步 = PR + PWA 前端消费(SC8)。
- 2026-06-21 **对标基线校准 (bao 钦定): 智能表格前端先对标 Grist (getgrist.com) 做功能一致, 再叠加飞书 Bitable 的 AI 智能表格能力。** 现状盘点 (真实代码 + `/table-lab` 视觉验证): 字段类型 25 种、8 视图 (Grid 已用 glide-data-grid)、filter/sort/group/隐藏/冻结/密度、AI 列、link/lookup/rollup/formula、条件格式、CSV 导入 —— route A 基本做完, `feishu-bitable-parity-assessment.md` 已过时。
  - **Grist 对标差距** (按 Grist 灵魂排序): ① Creator Panel 右侧三栏配置面板 ② Linked widgets / "Select By" 一页多 widget 联动 ③ Summary tables 作数据源 ④ Reference display-column + `$Ref.Field` 解引用 ⑤ trigger formula ⑥ 列宽/行高/换行 ⑦ DateTime/Integer 类型。Access rules / Raw data 多为 non-goal (单人)。
  - **增量 1 已落地** (working tree, 未提交): **Creator Panel 三栏布局** —— `SmartTableView` return 重构成 `tableShell > [tableMain, creatorPanel]`, 点列头/+Field 在常驻右面板配置该列 (复用 `SmartTableFieldEditor`, 不重造), 面板可折叠。tsc 绿 + vitest 136 绿 + `/table-lab` 视觉验证 + code-reviewer PASS。验证工具发现: `/table-lab` 路由能 headless 渲染智能表格 (不依赖 kernel), 是后续视觉验证入口 (浏览器 dev 模式连不上 :17872 WS bridge — 需 token, 只 Tauri invoke 可得)。
  - **下一步**: 增量 2 = Creator Panel 加 "Table" tab (把 filter/sort/group/fields 弹出菜单收进面板) + 面板内字段编辑器竖向排版打磨; 之后按差距清单推进 (DateTime 类型 / 列宽行高 / Reference 显示列)。
- 2026-06-21 **整体规划 + 路线确认 + 两轨并行推进** (bao「不要一个一个, 整体架构想清楚」+「两轨并行」)。整体规划图落 `smart-table-grist-parity-plan.md` (对标 Grist 主 + 飞书 AI 叠加, 三轨, 逐项进度)。**数据层路线确认 = 第三条 SQLite 派生索引** (bao 2026-06-21 reconfirm; 早在 ADR-002 §14 v30 已锁, 零 churn)。Track 2 完整实现设计落 `smart-table-relational-index-design.md` (5 切片 + DDL + index-backed RecordSource + 关系字段计算 + vim test 守护)。分支 `feat/smart-table-grist-parity` 已累积:
  - `66ca041` 轨1增量1: Creator Panel 三栏布局。
  - `728c469` 轨1增量2: filter/sort/group/fields 收进 Creator Panel 双 tab (Table/Column), 查询栏清爽。
  - `d8a7c50` **轨2 Slice 1**: kernel `smart_table_index.rs` SQLite 派生索引 store (st_tables/st_rows/st_cells EAV + value_num/value_date 派生列 + reindex_table/remove_table/is_fresh), 仿 vault_index.rs 教条, markdown 永远 truth, 纯附加零行为改动。cargo test 5/5 + 全量 kernel 绿 + checker PASS。
  - 全部三块均 tsc/cargo + 测试 + 视觉(前端)/单测(kernel) + 独立 code-reviewer PASS。
  - `4307045` **轨2 Slice 2**: `query_indexed` index-backed query —— number(gt/lt/gte/lte)/date(比较) 在 AND 下推 SQL 剪枝 + 同一个 run_query 权威过滤, **parity 不变式** (13-case 矩阵测 index 路径 ≡ 内存 run_query 字节级一致, 含未知字段拒绝)。number eq/date within/OR 故意不下推。参数绑定无注入。cargo 7/7 + checker PASS。
  - `cd59b4e` **轨2 Slice 3**: 索引接进 :17873 gate —— `SmartTable::query_via_index` (大表>500行走 index, 小表/无索引/任何索引错误降级 run_query) + `reindex_into` 写穿透 (produce 写 markdown 后刷新) + router st_index best-effort 打开。**markdown 永远赢**: 读时 content-hash freshness 检查 + 陈旧懒重建 + 故障降级; 异步 AI job/外部 vim 编辑靠读时 hash 漂移自愈。cargo 13/13 (vault_smart_table) + 全量 kernel 绿 + checker PASS。(注: 真实 :17873 curl smoke 待重建 app; handler 是已测 query_via_index 的薄接线。)
  - **轨2 索引地基完成 (Slice 1-3/5)**: store + index-backed query (parity) + gate 接线。
  - `0f4423d` **轨2 Slice 4a**: 关系引擎核心 (跨表 JOIN) —— st_refs 边表 + `index_references` (解析 link token, 按目标 display 字段解析 dst_row_id, 匹配不到=dangling NULL) + `compute_lookup` (沿边取目标字段, 多目标拼接, 跳 dangling) + `compute_rollup` (count/sum/avg/min/max over 关联 value_num)。**纯派生只读, 不写 markdown** (守 vim test)。dangling 目标重现可重解析 un-dangle。reindex 删 outgoing 边 + remove 级联 + incoming 置 NULL。SQL 全参数绑定。cargo 10/10 确定性绿 (顺手修了 fresh_index 并行 temp-path 碰撞) + checker 核验生产逻辑 PASS。
  - **轨2 Slice 4b** (committed next): 关系字段类型层 + 写保护 —— frontmatter 解析 reference(table/display)/lookup(via/target)/rollup(via/target/fn) 进 `SmartTable.relations` (对象 + inline flow 两形态), 计算列同时进 fields (可 filter/sort) 和 relations; `smart_table.describe` 广告 relations (Irisy 理解计算列); `is_read_only_field` + update_cell 拒绝写只读计算列 (reference 可写)。cargo 15/15 + checker PASS。
  - **轨2 Slice 4c** (一次性完成push): 查询时注入计算列值 —— `lookup_by_via`/`rollup_by_via` (复用 4a per-row compute + 按 via 单元格值重键, rollup 无重复计数) + `augment_relations` 跨表编排 (索引 source + 每个 Reference 目标表 + index_references, 按 row[via] 注入 lookup/rollup)。vault-backed e2e 测 (deals→contacts 注入 c_email/c_total 全通)。**纯派生不写 markdown**。
  - **轨2 Slice 5** (一次性完成push): 跨表 formula —— `RelationKind::Formula` + 小型安全算术求值器 `eval_formula` (+ - * / () + {field}/bare 引用, 除零/未知字段/解析失败→空白非错误数字, 无任意 eval, 递归下降优先级正确)。augment 在 lookup/rollup 后注入 formula (可引用计算列, e2e 测 half=c_total/2)。is_read_only 含 Formula。
  - **轨2 关系型灵魂 = 完成** (Slice 1-5): store → index-backed query (parity) → gate 接线 → 关系引擎 (Reference/Lookup/Rollup) → 类型层+写保护 → 值注入 → formula。Reference/Lookup/Rollup/Formula 经 :17873 gate 端到端可用, vault-backed 集成测验证, 全程纯派生守 vim test。checker PASS。
  - **轨1 = 4 项完成** (并行 agent): DateTime + Integer 列类型 / 多列 sort (SortKey[] + Creator Panel 有序排序键 UI) / 行高+文本换行 (Wrap text 开关 + glide allowWrapping)。tsc 绿 + vitest (smart-table 全绿) + table-lab 视觉验证。
  - **剩余诚实缺口**: 真实 :17873 curl smoke 未跑 (需重建运行中 app); kernel gate 逻辑由 vault-backed e2e 充分覆盖。前端关系字段的 frontmatter 写入对齐 (ctrl-web 当前客户端自算 relations, kernel 路径独立) 可后续统一。
- 2026-06-22 **协议探索旁支 → 回归主线 (bao「最初目标是不是被遗忘了,重新规划」)。** 本 session 偏离主目标做了一整轮通讯协议探索 (zeus 通讯协议角色), **零产品代码**。诚实定性: 是旁支, 但有沉淀价值, 成果归位、不再继续吃产品时间:
  - **ADR-010 communication** (新 module, 7→8, INDEX/PROCESS 已更新): CTRL 通讯总纲 = **统一窄腰 (§14 契约 + :17873 gate 治理 + MCP 插件) + 多元传输**; 8 缝传输选型; 质疑「一个框架统吃」(narrow-waist/CORBA-SOAP-ESB 教训)。**巩固了 §14 = 窄腰契约的地位** (正是本主目标要接前端的东西)。
  - **§14 开源战略研究 = 否定结论** (`protocol-opensource-strategy.md` + `research-protocol-2026.md`): §14 **不开源成标准** —— 想占的层 (agent↔数据操作 + 读写分离 + 写治理 + 语义层 + local-first) 2026 已被 MCP + GraphQL + Cube 语义层 + AI-governance 框架 + Obsidian-MCP 生态填满 (Apollo MCP Server 已做 mutation-需批准, Fast.io 已做风险分级治理, rodneydyer 已做 local-first MCP for PKM)。§14 无维度独特到拉起标准影响力。**§14 留作内部架构; CTRL 护城河 = 产品 (普通用户 local-first AI 平台 + 能力市场), 不是协议。** 要影响力则做 MCP 生态最佳 local-first 公民 (好想法作 MCP extension 贡献回去, 官方有 extension 机制)。
  - **协议校准沉淀** (ADR-010 Roadmap, 不阻塞主线): ACP 有据采用 (驱动 BYO-CLI, 阶段5) / ST-SS 向 AG-UI event 词汇对齐 (设计原则) / mesh 跟踪 Beelay-Keyhive (Automerge 官方下一代 E2EE+capability sync) / MCP 2026 增量 (MCP Apps sandboxed-iframe UI · Streamable HTTP Mcp-Method/Name headers 利好 gate)。
  - **重新锚定 (主目标 + Status 不变)**: 回 §14 + 智能表格 + 接 PWA 前端。**下一步 = SC8** (前端消费 §14 query gate: filter/sort/group UI + describe 驱动字段/算子 + AI 列 start→poll→展示) + 轨1 Grist parity 续 + 轨2 关系型索引续。协议探索期间撞 session 用量上限 (reset 4:10am PT), deep-research 后两轮 (§14 独特性/商业护城河) 未跑完, 但单线 WebSearch 已取得否定结论的关键证据。
- 2026-06-22 **全局规划落地 (bao「重新规划目标/竞品/远景/市场,把控全局」)。** 新建 `vault/ctrl/master-plan.md` = 全局把控真相源:远景 (普通用户 local-first 通用 AI 平台,卖 substrate 不卖模型) + 市场竞品定位 (CTRL 真空位 = 把 MCP 生态整合成普通用户平台,在产品层不在协议层) + **目标分层** (近期=本目标 SC8/智能表格 beachhead · 中期=能力市场+gate 治理 · 远景=能力生态) + 全局路线图 + 开发纪律。**目标调整结论: 主目标不改 (SC8 仍是正确近期 beachhead), 只抬进分层大图。** 读 GOAL=当前活跃目标, 读 master-plan=全局地图。接下来: 回 SC8 开发。
- 2026-06-22 **目标审查 (bao「目标和研究一致吗?会不会过时了?符合实际、能落地、循序渐进」)。结论: 架构方向没过时, 但进度记录严重过时——SC8 不是「下一步未做」, 而是已大部分落地。** git 实证: `9016c05 feat(sc8): unified query gate to PWA (#124)` (改 8 个前端文件: kernel.ts +82 / smart-table-gate-bridge / modules/smart-table +52 / viewer-registry) + `9426425 fix(sc8): grouping` + `728c469 Creator Panel filter/sort/group`; 前端代码已有 describe 驱动字段/算子 (`smart-table-query.ts` mirror kernel describe verb)、query gate 消费 (`useTableQuery.ts`)、AI 列 invoke (`kernel.ts:269 smart_table_run_ai_column`)。**研究与目标一致** (ADR-010 巩固 §14=窄腰契约; master-plan 定位智能表格=近期 beachhead; §14 不开源不影响内部落地)——所以**主目标方向不改**, 只是上面的「成功标准 7/8」状态过时, 以本 log 为准。
  - **真正的下一步 (符合实际/循序渐进)**: ① 验证并提交 working tree 未提交的 SC8+轨1 前端进展 (`git status` 有大量 modified: SmartTableCells/FieldEditor/Grid/View/useTableQuery 等); ② 走 dev-loop 确认 SC8 三块完成度 (tsc/vitest/table-lab 视觉) + 补 AI 列 poll-status 三件套展示若缺; ③ SC8 收尾后再二选一: 轨1 Grist parity 剩余差距 (Linked widgets/Summary tables/Reference 显示列) **vs** 进中期能力市场 (master-plan, 先拍 4 个 gate 决策)。不再「以为 SC8 没做而重起炉灶」。
- 2026-06-22 **下一阶段序列确定 (bao「1/2/3 都要做」)。验证坐实近期达成: typecheck EXIT=0 + smart-table 11 文件 67 测试全绿 (§14 query 15 / SC8 gate-bridge 4 / gate 消费 3 / 轨2 formula 6)。** bao 确认三方向都做——但守「一次一活跃目标」, 排成循序渐进序列 (beachhead 先扎实再扩平台):
  - **当前活跃 (最小, 先坐实达成)**: 闭合 SC8 验证缺口 = 真机 `:17873` smoke + `/table-lab` 视觉 e2e + 提交未交的 vault 文档 (master-plan/dev-playbook/mcp-capability-marketplace/protocol-research-summary)。
  - **阶段 B (智能表格 beachhead 完整化)**: 轨1 Grist parity 剩余 (Linked widgets / Summary tables / Reference 显示列) + 飞书 Bitable AI 智能表格叠加。
  - **阶段 C (从能力到平台)**: 中期能力市场 + gate 治理 (先拍 mcp-capability-marketplace 的 4 个 gate 决策 → 7 切片)。
  - 执行用 dev-playbook: 每阶段切 spec → 并行 subagent 实施 → code-reviewer 验证 → dev-loop commit。**开工前先 git/grep 核实真实状态 (本次教训: GOAL 进度会过时)。**
- 2026-06-22 **可视化验证 (bao「帮我可视化验证一下是不是真的完成了」)。结论: 前端真的完成了 (像素铁证), 真机端到端还差最后一截。** 方法: 现有 dev server :5173 + Playwright headless chromium 截图 `/table-lab` (`/tmp/table-lab.png`)。**眼见为实**: 完整智能表格渲染 —— 8 视图 (Grid/Kanban/Gallery/Calendar/Form/Summary/Chart/Timeline) + §14 query bar (FILTER Name/contains + SORT + GROUP, describe 驱动) + Creator Panel 三栏 (Table/Column tab + 密度 Cozy + Freeze + Wrap text + 9 字段可见性勾选) + 多字段类型 (文本/数字/单选彩色 pill) + 条件格式 (Amount 绿底 / lost 红底) + 5 行数据 + 聚合 (Amount SUM 60,800 / Seats SUM 196)。→ **SC8 前端 + 轨1 Grist parity 视觉确认达成。**
  - **诚实缺口 (可视化验证暴露)**: 截图时 page error `transformCallback` = 浏览器 dev 模式 Tauri invoke 失败 → `/table-lab` 是 **headless mock 数据渲染, 没连真 :17873 gate**。即「前端 UI」眼见为实完成, 但「前端 ↔ 真实 gate ↔ 真实 vault」**端到端仍需 Tauri 真机验证** (浏览器 dev 连不上 kernel, GOAL 早已记此入口限制)。
  - **进度小结**: 近期目标 = §14 内核 (✅测试) + 前端 SC8/轨1 (✅视觉) + 真机 gate e2e (⏳ 待 Tauri app)。「当前活跃」收窄为: **重建 Tauri app 跑真机 :17873 smoke + 真实 vault 数据的 /table-lab e2e**, 闭合这最后一截即坐实近期目标 100% 达成, 然后进阶段 B。
- 2026-06-23 **目标替换(SC8 §14 前端 → 通讯协议重构,两信任域)。** 理由:bao 连续多轮从「继续通讯模块重构讨论」→ 诊断出 ST-SS 是张力源 → bao 钦定 **ST-SS 全量弃用**(单向语义广播,做不了他要的多端远程控制)→ 真实业务 = **远程桌面(对标 ToDesk)**,WebRTC 栈,另立独立能力模块 → bao 要求先「梳理 ctrl 业务框架」(已落 `master-plan.md` §二·五 模块×对标 + §二·六 业务框架全景)→ bao 指令「先实现内部通讯和外部通讯,把通讯协议重构一下」。SC8 已基本达成(前端视觉铁证 + kernel 测试绿,差真机 e2e),让位为后续。**第一步 = SC1+SC2(内/外类型边界 + gate 审计 ledger)。** 走 dev-loop。
- 2026-06-23 **第一步落地(SC1+SC2 最小切片)。** gate 审计 ledger + 两信任域类型脊:`kernel/audit.rs`(`TrustDomain {Internal,External}` + sha256 args hash 守数据主权,只存 hash 不存全量)+ `persistence.rs` `audit_calls` 表 + `record_call`/`audit_count` + gate `call_tool` 抽出 `dispatch_tool` 包 best-effort 外部调用审计(记 caller/tool/args-hash/outcome/ts,ledger 写失败只 warn 不阻断调用)。**验证**:`cargo test --lib` 246 绿(含 `bearer_token_passes_auth_middleware`/`unauthorized_request_returns_401` 无回归)+ 3 新单测(domain/hash/ledger)。**独立 code-reviewer PASS,无 must-fix**(确认行为保持 + SQL 全参数绑定 + 全英文)。commit `ed8f30f`(docs)+ `255d45a`(feat)。**诚实缺口(范围外,非 FAIL)**:caller 恒 `"external"`(细分 client = SC3)/「编译器强制内核自调隔离」尚未做(= SC1 完整)/ 真机 `:17873` curl smoke 待 app build。**下一步候选**:SC3 可见性裁剪(也需 caller 细分 → 两者一起做)或 SC6 ST-SS 弃用。
- 2026-06-23 **通讯规范确认(自顶向下:业务架构 → 8 缝 → 行业对标 → 规范)。** bao 指令「拆解业务架构定通讯架构、找对标、确认 CTRL 通讯规范 + 全量补深度调研」。产出 `vault/ctrl/comms-interface-spec.md`(从 master-plan 业务架构逐缝长出接口契约,8 缝各带对标背书)+ deep-research(108 agent / 26 一手源 / 23 断言过 3 票对抗验证)。**调研背书的选型**:① Tauri Channels(非 Event)/ ② kameo OTP supervision / ④ Apollo MCP Server gateway 治理(flexible reads/controlled writes)/ ⑤⑦ ACP Registry 2026-01-28 标准 / ⑥ AG-UI transport-pluralistic / ⑧ Beelay/Keyhive content-blind+capability(pre-alpha 跟踪)。**调研推翻我的两个原裁决**:(1) 本机流不该用 protobuf —— Tauri Channels 原生二进制,protobuf 仅 scope 跨设备腿;(2)「单一 wire 横跨本机 IPC + 跨设备 P2P」**零先例** = 未验证赌注 → 正解 = **三动词语义契约 = SSOT(有先例),wire framing 按传输适配**。**诚实缺口**:远程桌面腿(RustDesk wire / 远控 E2EE / NAT 穿透)调研未覆盖 → 缝⑧ 专项 follow-up（独立目标)。ADR-010 amendment 清单已列(spec §4)。**下一步**:回 SC3(caller 细分 + 可见性裁剪,Apollo 背书)+ 适时落 ADR-010 amendment。
- 2026-06-23 **SC3 落地(caller 细分 + intent 可见性裁剪)。** 替换现状「全工具对所有 caller 可见」TODO,对齐 comms-spec 缝④(Apollo MCP gateway:flexible reads / controlled writes)。三块:`kernel/visibility.rs`(纯函数 `tool_domain` 域分类器 + `Intent` 按 `X-Ctrl-Intent` header 逗号分隔 capability 域投影,缺省/空 = unscoped 全集,`system` 域 kernel_status/vault_root_path 常驻)+ `audit.rs` `normalize_caller`(`X-Ctrl-Caller` 净化为 `[a-z0-9._-]` ≤64 字符,缺省 `"external"`,挡 ledger 注入)+ gate `mcp_server.rs`:`request_header` 从 `RequestContext.extensions` 的 `http::request::Parts` 读 header,`list_tools` scoped 时过滤,`call_tool` 记真实 caller(不再恒 "external")**且**拒绝越域工具(outcome `denied`,防御纵深 = 隐藏的也不可调)。**验证**:`cargo test --lib` 254 绿(含真 HTTP 端到端 `intent_header_scopes_tools_list`:真 initialize→tools/list 带 `X-Ctrl-Intent: vault`,断言只返回 vault/system 工具 + http_post 被隐藏 —— 坐实 header→Parts→context 穿线,非纯函数)+ 7 新单测(6 visibility / 1 caller)。**独立 code-reviewer PASS,无 blocking/should-fix**(确认非破坏默认放行 + 双面强制 + 全英文 + 无注入/无 arg 泄漏)。**诚实缺口(范围外,非 FAIL)**:projector 尚未为外部 CLI 自动 stamp `X-Ctrl-Intent`(外部调用默认 unscoped 直到 producer 设 header,= 文档化迁移姿态,bearer-on-loopback 仍是主 gate)/ 真机 `:17873` curl smoke 待 app build(但 in-process reqwest→真 axum listener 已是真 over-the-wire 铁证)。**下一步候选**:projector stamp intent(让裁剪对外部 CLI 默认生效)或 SC6 ST-SS 弃用或落 ADR-010 amendment。
- 2026-06-23 **SC3 闭环(projector 给外部 CLI stamp caller+intent)。** 闭合上条「诚实缺口」—— `kernel/projector.rs` 写 `.mcp.json` 时,`kernel_entry` 的 `headers` 现在除 `Authorization` 外多 stamp `X-Ctrl-Caller: byo-cli`(driver-agnostic,进审计 ledger 可归因)+ `X-Ctrl-Intent: <默认 scope>`。**默认 scope = `vault,smart_table,notes,providers,registry,kv,llm,memory`**,**故意排除 `net`**(http_get/post 外泄面 —— BYO CLI 自带网络,经 CTRL gate 反而造一条贴 CTRL 标签的未审计外泄路径)**和 `mcp`**(原始下游透传 —— BYO CLI 自挂 MCP),`system` 永远可见。**逃生舱** env `CTRL_BYO_INTENT`(逗号域列表 / `unscoped` 关裁剪)。header 名复用 gate 同一组常量(`audit::CALLER_HEADER`/`visibility::INTENT_HEADER`),无漂移。**这是有意的行为变更**:外部 CLI 从此默认只见 CTRL 数据增强工具子集(对齐 ADR-010 §G capability-based 最小暴露面 + 数据主权护城河)。**验证**:`cargo test --lib` 258 绿(+4 新 projector 测试:header stamp / unscoped 省略 intent / 默认排除 net+mcp 且真隐藏 http_post / e2e project_into_dir 写 caller+scope)+ 用户手加 server/未知 key 仍保留(merge 逻辑未动)。**独立 code-reviewer PASS,无 blocking**(确认 round-trip 无漂移 + 测试不改全局 env 无竞态 + 全英文)。**诚实缺口**:`resolve_byo_intent` 的 env 分支未单测(避 env 竞态,纯函数输出已覆盖)/ 真机 `:17873` curl smoke 仍待 app build(此步是纯文件投影单元,gate round-trip 已由 SC3 e2e 覆盖,常量共享)。**SC3 = 完整**(gate 裁剪 + caller 细分 + 外部 CLI 默认受裁)。**下一步候选**:落 ADR-010 amendment(SC7,把两信任域 + caller/intent 治理 + ST-SS 弃用入列与实装对齐)或 SC6 ST-SS 弃用。
- 2026-06-23 **SC7 落地(ADR-010 v4→v5 amendment,实装对齐)。** 守「ADR 跟实装不允许漂移」,把 SC1-3 已落地事实 + 已决策待实施方向写进 ADR-010(section amendment,按 PROCESS.md §1:改正文 + bump version + 追加 changelog + 改 last_updated,不开新 ADR)。五处:(1) **§ trust-domains 加实装锚点表** —— SC1/2/3 代码落点(audit.rs/visibility.rs/projector.rs/mcp_server.rs)+ 诚实缺口(`InternalMsg ⊥ GateRequest` 编译期隔离仍是运行时 tag)。(2) **§G 可见性裁剪 从「TODO 治理半成品」→「已落地」**(caller 细分 + intent 投影 + projector stamp,对标 Apollo gateway)。(3) **§ transports ③⑥ ST-SS 弃用 → Tauri Channels + WS**(bao 钦定,单向语义广播做不了多端远控;SC6 实施,决策入册)。(4) **§ transports ⑧ + § internal-external 调研修正**(二进制流帧走 Channels 原生,protobuf 仅 scope 跨设备腿;单一 wire 横跨本机+跨设备 = 零先例未验证赌注 → 三动词语义契约 SSOT + framing 按传输适配)。(5) **远程桌面转正独立能力模块**(对标 ToDesk/RustDesk,⑧ 缝留口,远控腿待专项调研)。Roadmap 加 stage 0.5「两信任域重构 SC1-7」。**同步** INDEX.md(stale v2 → v5 + 摘要刷新)。纯文档无 code,事实源 = `comms-interface-spec.md` §4 amendment 清单。**通讯重构本目标 SC1/2/3/7 = 完成;剩 SC5(136 command 收敛策略)/ SC6(ST-SS 弃用实施)。** 下一步候选:SC6 ST-SS 弃用(本机流改 Channels+WS,前端流不回归视觉验证)或 SC5 command 收敛策略。
- 2026-06-23 **SC6 范围勘查(系统设计先行,未动码)。** 4 路并行勘查 + 亲核关键依赖,落 `vault/ctrl/stss-deprecation-scope.md`。**核心 reframe:ST-SS 是两个风险截然不同的东西**:**半 A = TS 协议抽象死重**(`@ctrl/stss` + 唯一消费者孤儿 `@ctrl/memory`;**ctrl-web 零 import,直接 `cbor-x` 解码** → 与 PWA 解耦;删整包零产品影响,只掉协议参考测试)；**半 B = kernel `:17872` 活线**(载 3 个出货功能:LLM 逐字流 `McpOutputPane` / xterm 终端 `code-space` / 结构化 cell 右栏;删了视觉回归 = 红线)。**关键洞**:当前 `:17872` 本就是 token-WS 载 CBOR Cell/Op,所谓「ST-SS」活线 ≈「最简 WS」,真死的是没人用的语义流抽象(reducer/capability envelope)→ 半 B 是**去抽象+改名+保 payload 契约**,非重建流。**计划**:Phase 1 删半 A(零风险,即坐实 SC6「ctrl-stss 移除」)→ Phase 2 半 B 去抽象/改名(`subprocess_stss_adapter` 内联进 `code_space`、`stss_bridge` 降级改名保 `:17872`+token+payload、前端枚举改名;**需真机视觉验证** 3 功能不回归)→ Phase 3 可选 Tauri Channels 迁移(独立小目标,browser dev 测不了)。`share/stss-spike` 已不存在(topology 文档 stale)。**待 bao 拍**:SC6 做到档 1(推荐:删半 A + 半 B 去抽象,保 token-WS 当最简 WS)还是档 2(再迁 Channels)。
- 2026-06-23 **SC6 Phase 1 落地 + 旁支战略产出(应 bao 提问)。** **Phase 1 删死重 TS 包**:`@ctrl/stss` + 孤儿 `@ctrl/memory` 整删(全仓扫描确认零外部引用,ctrl-web 用 cbor-x 不 import),lockfile 更新,topology 文档修 stale(stss-spike 已不存在 + 测试数);ctrl-web typecheck + 139 测试绿,零回归(commit `f47d3ec`)。**旁支(bao 连续提问拉出,均落 vault)**:① `feishu-mcp-research.md`(deep-research 101 agent/22 确认:飞书有官方 MCP `@larksuiteoapi/lark-mcp`,Bitable 全读写,OAuth+keychain local-first 友好,可零自研挂 gate;社区第三方全玩具;无 agent CLI = 印证 CTRL 定位)② `capability-pack-map.md`(应 bao「ctrl 要实现哪些外部功能包」—— 三层功能包统一图:①原生模块4 + ②内置mcp~15 = CTRL 自建;③外部 connector 长尾故意不建,只种子;**种子优先级我拍:Tier1 = 飞书(零建放大 smart-table 滩头)+ 开源 CRM(小建证数据主权护城河)**,待 bao 否决)。**诚实定性**:Phase 1 是 SC6 真进展;后三个 doc 是战略旁支(有沉淀价值,但偏离 SC6 主线)。**下一步**:回 SC6 Phase 2(半 B 去抽象,需真机视觉验证)或先拍 capability-pack-map 的种子决策。
- 2026-06-24 **端点 spec 物化 + SC5 棘轮 lint 落地(应 bao「完整协议含端点 / 有规范吗 / 没按规范走还是不会建」质疑链)。** ① **协议模型收紧(ADR-010 v6)**:WebSearch 核实(OpenAPI/AsyncAPI/gRPC/MCP 均形式化定义端点)→ 新增 § endpoint-spec:协议 = §14 语义 SSOT + 每缝标准 wire(工具=MCP/流=AsyncAPI/跨设备=protobuf)+ gate;**绝不自造 wire/IDL**;权威端点 spec = 导出物;标准~90%/自创~10%。② **导出 MCP schema 当权威端点 spec**:`KernelMcpRouter::export_tool_schemas()` + bin `dump_mcp_schema` → `vault/ctrl/mcp-schema.json`(54 工具完整 JSON Schema 2020-12,从 rmcp 宏非爬源);catalog 改成读 schema 不爬源(闭环)。③ **SC5 棘轮 lint**:`scripts/ratchet-dual-surface.mjs`(读源码算 MCP tool ∩ Tauri command 名)锁双表面基线 **31**,进 `.husky/pre-push`,挡新增双表面(迁一个降一个);证实 30→EXIT1 / 31→EXIT0。**关键探明**:31 双表面是**两套实现 + 治理分叉**(Tauri `vault_read` 做 check_cap,MCP 不做)= ADR 诊断的 P1 真漂移,非装饰。commit `f589d80`(catalog)+ `d181e0b`(ADR v6)+ `44ff342`(schema 导出)。**SC5 = 策略+棘轮已立**;**§14 盖全 = 范式迁移待**(下一步:迁一个 cluster bespoke→§14 source 当范式,基线降)。
- 2026-06-24 **系统设计先行(应 bao「你在设计系统还是只实现功能」)。** 诚实定性:这阵子偏向「实现功能/反应式切片」(你问一块我答一块),非自上而下执行一张顶层图 = patch-quilt 信号。**纠正**:停切片,把散在 5 处(ADR-010/comms-interface-spec/endpoint-catalog/capability-pack-map/SC1-7)的通讯设计**收敛成一张完整执行图** `vault/ctrl/comms-system-design.md`:目标终态(一张图:§14 SSOT + 标准 wire 分缝 + gate;`gate_invoke` 桥让前端经 gate 调能力)+ **全量端点分类**(188=54 工具+134 command 全归属:数据源→§14 source / Effect→§14.9 / 流→AsyncAPI / **app-shell 合法 Tauri-only 不 §14 化** / 跨设备→protobuf)+ **依赖排序迁移图**(A 地基✅ → B 退面[gate_invoke+退 31 双表面,降 ratchet] → C §14 盖全[39 bespoke] → D 流 AsyncAPI[SC6] → E 跨设备)。ADR-010 仍决策 SSOT,本图统管执行。**接口达产品标准 = B+C+D 完**。下一步 = 执行 B(建 gate_invoke 桥 + 迁 smart_table_query 退面,基线 31→30)。
- 2026-06-24 **全量开发推进(bao「全量做开发,遵守 harness 开发原则」)。3 个 dev-loop 验证切片:** ① 清死 Pi PATH 引用(`38009cf`,硬规则审计的唯一 cruft)② **信任域审计边界类型化 `GateRequest`**(`5984af3`,SC1 ledger 半边 —— record_call 只收 GateRequest,内部无构造器 → 编译器挡「内核自调进审计」;independent checker PASS;诚实:dispatch 路径 + InternalMsg sibling 类型仍 SC1 follow-up)③ **§14 vault Text profile**(`19af3b1`,vault 内容搜索成 §14 source:`text::{describe,query}` + vault_text_describe/query 网关工具,补 NotesSource 文档提的 TextSource 缺口)。**§14-vault 读 profile 现已覆盖**:Record(notes_query,含 HasTag)+ Text(vault_text_query)。全程 cargo 263 绿、ratchet 31 中性(新工具无 Tauri 孪生)、schema/catalog 重生成。**诚实墙(再次确认)**:剩余 §14-vault(produce 形式化 + 退掉 bespoke vault_* 双表面)+ 最大头漂移(#1 双表面收敛 / #2 前端绕 gate)**本质 frontend-coupled,安全推进需真机视觉验证**(browser-dev 连不上 gate)。继续盲做 = 投机平行表面 / 盲改前端 = 降质量,不做。**ADR 符合度**:硬规则零违反;§14 覆盖从 1 模块深 → vault 读 profile 入列;信任域从运行时 tag → ledger 编译期化。
- 2026-06-24 **平台一致性收尾(bao「全量干」+「退役 fallback 命令收尾」+「打开 ctrl 看看」)。** 全量执行 + 退役 + 清理三连:① **前端全量迁 gate**(`a37b762`)—— kernel.ts ~28 能力 wrapper + IrisyChat/viewer-uri 散点 → gateInvoke(经 :17873 gate),跳过 3 个无 MCP 孪生的(vault_watch_recent/irisy_soul_*/irisy_question_vault);typecheck + vitest 142 绿。② **退役 29 双表面命令**(`0e98969`)—— unregister + 删 commands/vault_embeddings.rs 整文件;**dual-surface 31→2**(剩 kernel_status/vault_write_image,无迁移前端 caller);ratchet baseline 31→2。③ **删死码收尾**(`d668c0a`)—— 删 24 个孤儿 vault 命令体 + 结构 + helper + 清 import,cargo **零警告**、263 test 绿。**可视化验证**(「打开 ctrl 看看」):vite :5173 + Playwright 截图 home + /table-lab —— 渲染干净(版本 pill / Irisy / 8 视图智能表格 / Creator Panel 全在),证明迁移没破坏前端;browser-dev 连不上 kernel(预期),数据路径需真机。**架构符合度跃迁**:#1 前端绕 gate + #2 双表面 = **代码层关闭**(CTRL 现在是平台:前端是自己平台 API 的一等客户,所有消费者过同一道 gate)。**关键诚实缺口**:gate_invoke 的**运行时**(前端 JS→Tauri invoke→loopback gate)**未真机验证,且已退 fallback = 无退路** —— 若真机有 bug,命令在 git 历史可恢复 + 修 gate_invoke。**下一步**:真机点核心(尤其智能表格关系列)确认;其余 ADR 缺口(§14 produce / ST-SS 退役 / 跨设备 / 市场 4 安全)仍待。
- 2026-06-25 **真机阻断根因修复 + 两信任域重构全量收口(bao「智能表格 new 都不能用 / 你是用 harness 管线开发吗 / 全量重构」)。** 上条留的「gate_invoke 运行时未真机验证 + 已退 fallback」赌注**真机炸了**:智能表格新建 / note 保存全坏。**纠正开发方法**:从读码+curl(对 UI 瞎)切到 **Playwright harness mock Tauri IPC → 真实 :17873 gate** 当开发+验证手段,每步 PASS 再退 fallback。① **根因(我的迁移 bug)**:前端 `vaultWrite` 送 `content`,但 gate 的 `vault_write` 要 `body` → serde 丢必填字段 → **gate 拒收每个写**(新建表/存 note)。修 `kernel.ts`+`viewer-uri.ts` content→body + 回归契约测试;harness 真机验证 createSmartTable/vaultWrite PASS(`df0e77b`)。② **SC1 完整体**:`InternalMsg` 正类型(`audit.rs`,Internal-by-construction,包 Event+origin,与 GateRequest 双向无互转)+ `EventBus.publish` 只收 InternalMsg(`event.rs`,broadcast)→ 编译器两端挡跨域;v5 编译期隔离缺口关闭(`aed02d1`,269 test 绿)。③ **SC3 关默认敞口**:无 intent header 不再全开 —— `default_for_caller`(第一方宽集减 net / 未知 minimal),`mcp_server` list+call 两处接;真机验证未知 caller 被拒 vault_read、tools/list 只剩 system、pwa 见 54 工具(`d7ded35`)。④ **SC5 收敛**:irisy_soul 退到 gate get/set + 修 3 处孤儿 invoke(打退役命令的 SOUL 提示词 bootstrap/load 本就坏);命令 106→104(`4f93ab0`,harness PASS)。⑤ **SC6 P2**:ST-SS de-brand 为 plain CBOR-over-WS + 退死命令 publish/list_streams/get_bridge_token,留载重 subscribe(`00b45ed`,命令 104→101)。⑥ **SC7**:ADR-010 v6→v7,实装锚点表 + 诚实缺口收口对齐。**通讯重构 SC1/2/3/5/6/7 = 落地**;残留(诚实):SC4 actor handler 签名 retyping(窄)/ SC6 符号改名(churn,待视觉验证)/ §14 produce review gate / 跨设备腿。**方法教训**:退 fallback 前必须 harness 真机验证字段契约 —— content/body 这类 shape 漂移单测抓不到,只有真机 gate 抓得到。
- 2026-06-30 **目标替换(通讯两信任域重构 → LifeOS layer 重构)。** 理由:通讯重构 SC1/2/3/5/6/7 已落地(gate 治理 + 两信任域类型脊 + ST-SS de-brand + command 收敛),bao 2026-06-30 指令「根据 aino lifeos 重构 ctrl 前后端,仔细认真」+「远程要有」。**开工前先系统设计**(守硬门):4 路内部调查(前端图/后端图/为什么没远程/ADR 锁点)+ `deep-research` skill 多源网调(102 agent / 20 源 / 83 主张 / 25 对抗验证)。**定盘发现**:「Aino」是两个产品——aino.md 独立 app vs lifeos.vip 的 Obsidian 插件(bao 贴的付费分层属后者);后者无自带编辑器、靠 raw 子进程管道缝 CLI、无 gate。→ **CTRL 地基更强,重构 = 嫁接 LifeOS 功能面 + 补远程,不推倒**。新目标分 5 阶段(Life 数据模型 → PARA/periodic → Calendar → 上下文管道+IA → 远程),governing = `lifeos-layer-restructure.md`。**下一步 = Phase 1 SC1**:Task 作 §14 Source(describe/query/produce + 明文落盘 + gate 暴露),走 dev-loop。
- 2026-06-25 **文档数字对齐 + 死包清理(SC5 精确化 + opencode 下线 + olym-core 删除)。** ① **SC5 数字精确化**:GOAL 旧写「136 Tauri command 收敛策略」与实装漂移 —— 实装实际 **105 个 command**(已落地一轮收敛:ST-SS surface 4→1、vault 21→5、pi/hermes_chat/opencode_chat 等退役;差额 31 = 已退役的 dual-surface command,`mod.rs` 有逐条注释)。把 SC5 + 非目标的「136」更新为准确描述:收敛范式 = `gate_invoke` 单一治理通道(有 MCP twin 的命令退到 gate),收敛已基本完成。② **opencode 下线**:真相源原写「opencode 未接线(保留作未来 coding 路径)」,实装曾全栈接线(installer/launcher/coding 路由/UI),bao 2026-06-25 裁决 opencode 下线回归未接线,代码已移除 —— 同步更新 `architecture-byo-cli-driver.md` / `CLAUDE.md` 描述 + DRIFT.md 记一行(resolved)。③ **死包删除**:`packages/olym-core/`(已坏 export + 无 package.json + 全仓零 live 引用,olym 多智能体框架已 2026-06-19 剥离)整删 + 清 package-lock.json extraneous 条目(workspaces 是 `packages/*` glob,无需改数组)。纯文档/死码清理,零产品代码改动。
- 2026-07-01 **功能包产品级化「全量修复」启动(bao「我们的功能包,类比 project 的话,都是产品级别的;也是用户创造中心,分享中心」+「全量修复」)。** 调研 Atoms(MetaGPT, 克隆 `~/Documents/coding/metagpt-reference/`)+ Lovable —— 两者都以 **project** 为顶层单位(MetaGPT `Team.generate_repo(idea)→repo` 的 Environment+Role pub/sub 内核**独立收敛到 CTRL 5 primitives** = spine 外部印证;Lovable 两级 knowledge = `CLAUDE.md`+`AGENTS.md` 同构)。**CTRL 的答案 = 功能包当产品级单位**,substance 反转(整合非重造 / MIT commons 非托管 / 本地非云)。**ADR-002 §7.5 v41**:功能包三属性锁(产品级 §14 uplift + 创造中心 Irisy mcp-creator + 分享中心 registry/commons)+ 读法 A(功能包=①原生+③connector 产品级;②Top15 原子工具=配料非功能包)。**架构调整清单**(前端+后端 Explore 摸底):底座已完整(provision/auth/query 引擎 + registry-Discover + PackCreator NL→manifest 都在),缺 3 处定向抬升 —— P0 通用 §14 源(§14 uplift 现每 connector 手写 gate 工具=非零代码)/ P1 后端 `mcp_pack_create`(brain 侧创造空)/ P2 发布侧(只 JSON copy-paste,无 registry publish,碰 v1/v1.1 决策)+ P3 per-pack 投影作用域。**P0 落地 2 片(dev-loop,绿+独立 checker PASS)**:① **ADR-002 §14.12 v42 + `kernel/manifest_source.rs`**(通用 manifest-driven §14 connector source:`RecordSourceSpec` + `ManifestConnectorSource: QuerySource`,复用 `run_query`+`mint_bearer` 零重造;9/9 测试含 ghostfolio 等价证明 + mock-HTTP fetch/produce;commit `a1e1041`)② **§14.12 `record_source` manifest 轴 + ghostfolio manifest.json 声明**(TS Zod schema,enum 精确匹配 kernel serde;ghostfolio §14 shape 现纯数据;typecheck + vitest 3/3;commit `a3a76dc`)。版本 →0.1.736。**诚实缺口(下一片 P0-2b)**:通用 gate 工具 `source_describe/query/produce(source_id)` 接线 + 从 `auth.token_exchange` 建 spec + 退役手写 `ghostfolio_describe/query/add_transaction` + 前端 FeaturePackScene 产品级视图(P0-2 scene)尚未做;真机 gate smoke 待 app build。**决策待 bao**:P2 分享中心 = 拉进 v1 还是守 ADR-001 §1B.6 的 v1.1?P3 per-pack 投影作用域(碰 ADR-001 §projection + Channel)现在做否?
- 2026-07-01 **P0 后端完成 —— 通用 §14 connector source 成唯一 SSOT(dev-loop 续,「继续」)。** 接前条 P0 slice 1-2,再落 2 片闭合 P0 后端:③ **通用 gate 工具接线**(`2d33d28`)—— `source_describe/query/produce(source_id)` 三个通用工具经 :17873 gate,`manifest_source::spec_from_manifest`(manifest→spec + 复用 `auth.token_exchange` 映射字段名)+ `resolve_pack_creds`(通用 creds,base_url 从 `_base_url`、token 从 manifest 声明的 send_secret,kernel-side 不进 LLM)+ visibility 新 `source` 域;cargo 343 绿(含真实 manifest spec 测试 + over-the-wire 可达性)、ratchet 2/2、独立 checker PASS(唯一 note:dual SSOT 须下片关)。④ **退役手写 ghostfolio 路径**(`6c4a0c8`)—— 删 `ghostfolio_source.rs` 整模块 + `ghostfolio_describe/query/add_transaction` 工具 + args + `resolve_ghostfolio_creds` + visibility ghostfolio 域 + e2e 测试;ghostfolio §14 shape 现全在 manifest `record_source`,**通用引擎成唯一实现**;cargo 333 绿、零残留 ghostfolio_ 引用、ratchet 2/2、schema 76 工具、web vitest 3/3。版本 →0.1.739。**净结果(命题证成一半)**:加一个 REST connector = **纯 manifest 数据 + 通用工具,零 per-connector Rust** —— 产品级(§14 uplift)+ 零代码两件事端到端成立,且**一个 SSOT**(无手写/通用双轨)。**P0 后端 = 完成**。**诚实缺口**:真机 curl(需 bao 机器跑 Ghostfolio 实例)是同一既有 gap,in-process over-the-wire 是诚实替身;per-source per-call 授权 = v1.1(现 tool-level 门,对第一方安全)。**下一步 = P0 前端(task #2)**:FeaturePackScene 从 action-bar 升级为读 `source_query` 结果渲染的产品级视图(ghostfolio=组合/持仓),需 Playwright 视觉验证。之后 P1(mcp_pack_create)/ P2(分享,待 bao 拍 v1-vs-v1.1)/ P3(per-pack 投影作用域)。
- 2026-07-01 **P0 完成 —— 产品级视图前端落地(dev-loop 续,「继续做 P0 前端」)。** ⑤ **前端产品级记录视图**(`5e2f8ea` + key 修复 `53a8d72`):功能包声明 §14 `record_source` → 场景以**产品级数据表**领衔(Ghostfolio=持仓表),不再是一排按钮 —— `SourceDataView`(数字列右对齐+tabular figures,hairline 编辑风)+ `FeaturePackScene` 双 face(有 records=数据表[开场加载/Refresh/produce 后重取],无=旧 action-bar 无回归,记录加载注入式 `loadRecords` 镜像 `onRunAction`)+ `describeSource/querySource` gate 绑定(复用 smart-table 类型,非平行 SSOT)+ `loadPackRecords`(describe+query→SourceData)+ AmbientHome 接线。**视觉验证**(Playwright `/pack-lab` DEV 路由):截图确认持仓表 4 行 + Quantity/Value/Allocation 右对齐 + action-bar face 无回归。typecheck 绿 + records 单测 1/1 + manifest 3/3;独立 checker PASS(亲自复跑 typecheck+vitest 4/4,type 端到端对齐,JSX 自动转义无 XSS,useEffect deps sound)。版本 →0.1.742。**P0(后端通用 §14 source 一个 SSOT + 前端产品级视图)= 完成 —— 「功能包=产品级单位」命题端到端证成**(加 REST connector = 纯 manifest 数据 + 通用工具,零 per-connector 代码,且以产品级数据视图呈现)。**诚实缺口**:真机 live fetch 需 kernel(browser-dev 连不上 :17873,既有 gap);`irisy-prompts` 2 失败测试 = 分支预存 persona v13/v14 漂移(stash 验证与本改动无关,属 Irisy persona 范畴不在本切片)。**剩余「全量修复」**:P1 后端 `mcp_pack_create`(brain 侧创造,mcp-builder 四阶段+evals)· P2 发布/分享侧(**待 bao 拍 v1 vs ADR-001 §1B.6 的 v1.1**)· P3 per-pack 投影作用域(碰 ADR-001 §projection+Channel,待 bao 拍)。
- 2026-07-01 **P1 完成 —— brain 侧功能包创造经 gate evals(dev-loop 续,「全量」)。** 补「brain 能 author 功能包」缺口的正解:brain 用**自己的模型**生成 manifest,再经 gate **评估**后再装(mcp-builder Phase 3 review + Phase 4 evals;研究点名 Phase 4 evals = 家酿都跳的质量护城河)。kernel 保持薄:它评估,brain 生成。⑥ **后端 `mcp_pack_validate`**(`aec34b6`):`pack_validate.rs` 纯函数 `validate_manifest→ValidationReport{ok,issues[{field,severity,fix}]§14.11 自纠形,record_source_fields}` —— 查 id/version、功能包须 DO something(actions[] 或 §14 record_source §7.5)、record_source 连贯性(复用 P0 `spec_from_manifest`:parse/fields 非空/endpoint/produce body/auth warn)+ 正向 describe eval;gate 工具 read-only;cargo 340 绿含 7 pack_validate 测试(真实 ghostfolio manifest 过 + 6 字段 describe eval;bad id/no-action/empty-fields/bad-operator 全 fail-closed)。⑦ **前端 PackCreator evals + edit-JSON**(`aa6bc62`):生成后自动 validate + install 门控在 evals(有 error 禁装「Fix errors to install」)+ Edit JSON 手改 + Re-check;`PackEvals` 呈现组件(绿过/红 error/琥珀 warn,等宽字段名+fix 提示);**视觉验证** Playwright `/pack-lab` 三态渲染正确 + record scene 无回归;typecheck 绿 + web vitest 181 过(仅预存 irisy-prompts 2 失败)。版本 →0.1.746。**P1 = 完成**。**剩余「全量修复」**:P2 发布/分享侧(**bao 拍:拉进 v1 含 registry 发布** → 改 ADR-001 §1B.6 把共享网络提前)· P3 per-pack 投影作用域。
- 2026-07-01 **P2 完成 —— 分享中心发布侧拉进 v1(bao 拍「拉进 v1 含 registry 发布」)。** **ADR-002 §7.6 v43**:pack-PUBLISH 半边(§7.5 属性 ③ 分享中心)入 v1;精确不越界 —— 是 pack 市场生产者(manifest 发到 registry),**非 §1B.6 mesh 投影网络**(仍 v1.1,mesh substrate 依赖);发 manifest 是纯 HTTPS 无 mesh 依赖故只此半边移。⑧ **后端 `mcp_pack_publish`**(`3b6d2f5`):`pack_publish.rs::publish(manifest,url,token)` —— **evals first**(复用 `pack_validate`;有 error 返 `Blocked(issues)` 且**不发 HTTP**)→ POST manifest → 容错 `PublishRef{id,namespace,url}`;gate 工具 + `resolve_registry_creds`(endpoint+token 从 `ctrl:registry:publish_url/:publish_token` 或 env,kernel-side);cargo 343 绿 + 3 pack_publish 测试(broken 在 HTTP 前拦 / valid mock-HTTP 发布返 ref / registry error 类型化);schema 78。⑨ **前端 Share 动作**(`05427f5`):scene header 加 Share 按钮 → `publishPack` → 显示发布 ref(namespace/url)或错误 banner;JSON copy-paste 留作零基建 fallback;typecheck 绿 + 视觉验证(Share 按钮两 header 渲染无回归)。版本 →0.1.751。**create→publish→discover 一 v1 环闭合**。**诚实缺口**:真实公共 registry 需 mcp-publisher namespace + live endpoint(外部 gap,mock-HTTP 验);真机发布需 kernel + 配置 registry。**P2 = 完成**。**剩余**:P3 per-pack 投影作用域(碰 ADR-001 §projection + Channel)。
- 2026-07-01 **P3 完成 —— per-pack 投影作用域(功能包 = project-scope,「全量修复」收尾)。** **ADR-002 §1B.8 v44**:把 §7.5「功能包 = CTRL 对标 project」在投影层兑现 —— 每个 pack 成自己的投影作用域,BYO-CLI 在 pack 目录启动即只见该 pack 能力+上下文。⑩ **`projector::project_pack`**(`554ebff`):物化 pack-scoped `.mcp.json`(盖 pack **自己的** intent 域 —— §14 数据包 → `source`,**非全局默认**;优雅推论:pack 精确授予自己域**而不拓宽 base workspace**,因 `source` 故意不在 BYO 全局默认里)+ pack-context `AGENTS.md`(name+KB+「scoped to this pack」)到 `~/Documents/CTRL/<pack_id>/`。两处非破坏 refactor 复用 base 机制(`project_gate_into_dir` 显式 intent / `project_agents_block` 显式 block,同原子写+marker 保留+幂等)。boot 触发 `project_installed_packs` 为每个已装 record_source 包自动投影(action-only 包 scope 推导模糊,推迟)。**非新 primitive** —— 用 ADR-010 intent/visibility 当作用域命名空间(概念上 Channel 当 per-pack scope),5 primitives 不动;不拉 §1B.6 mesh 网络(仍 v1.1),单用户本地 per-pack。cargo 347 绿含 4 新 projector 测试(per-pack gate 盖自己 intent 非全局默认 / pack_agents_block 名+KB+scope / 无 KB 省略 / AGENTS.md 保留用户散文)。版本 →0.1.753。**诚实缺口**:真机 boot 投影需 running kernel + 磁盘已装包(机制用 temp dir 单测,HOME-based boot 是诚实集成 gap)。**P3 = 完成**。
- 2026-07-01 **「全量修复」全部完成(P0-P3)。** bao「我们的功能包 = 产品级/创造中心/分享中心」+「全量修复」的整条链端到端落地(10 commit / 版本 →0.1.753 / 每片 dev-loop 三层验证 + 独立 checker PASS):**P0** 通用 §14 connector source(加 REST connector = 纯 manifest 数据零 Rust,一个 SSOT)+ 前端产品级记录视图;**P1** brain 侧创造 evals(`mcp_pack_validate` + PackCreator evals/edit,mcp-builder review+evals);**P2** 分享中心发布侧(`mcp_pack_publish` evals-first + Share 动作,create→publish→discover 一环);**P3** per-pack 投影作用域(功能包=project-scope)。ADR-002 §7.5/§14.12/§7.6/§1B.8 四处 amendment(v41-v44)。**命题证成**:功能包 = 产品级单位(§14 uplift)+ 创造中心(gate evals 造)+ 分享中心(registry 发布)+ project-scope(per-pack 投影)。诚实缺口贯穿:真机路径(真实 Ghostfolio 实例/真实 registry/running kernel boot)需 bao 机器,CTRL 侧全 in-process over-the-wire + mock-HTTP + 视觉验证坐实。
- 2026-07-01 **P1+ 落地 —— OpenAPI→§14 record_source scaffold(AutoMCP,「继续 P1」)。** 补 P1 原计划的 scaffold 阶段(mcp-builder Phase 2 + AutoMCP)。研究定盘:codegen 基本已解,**瓶颈是 spec 质量非 codegen** → 生成 best-effort 草稿 + spec-repair notes,作者 edit + evals 后再装(不假装完美)。⑪ **后端 `kernel/openapi.rs` + gate `mcp_pack_scaffold`**(`af3b02e`):纯函数 `record_source_from_openapi(spec,path,method)→Scaffold{record_source,notes}` —— 解本地 $ref、定位 row array(裸 array 或命名 array 属性)、item 属性→§14 field(type 映射 integer/number→number、bool→checkbox、string+date→date、uri→url)、camelCase/snake→Title Case label、notes 标记 security(加 auth)/缺 schema/未知类型;read-only;直接喂 P0 record_source(指向一个 API → 得 connector 草稿);cargo 353 绿 + 6 openapi 测试;schema 79。⑫ **前端 PackCreator「From OpenAPI」路径**(`6a08346`):`<details>` OpenAPI spec + path → `scaffoldFromOpenApi` → 草稿 manifest 落进 JSON 编辑器 + 显示 notes → 走已有 evals/edit/install;draft-only 按钮(Edit-JSON/Regenerate)对 scaffold 隐藏(无 NL 草稿);typecheck 绿 + web vitest 181 过。版本 →0.1.757。**AutoMCP scaffold→refine→evals→install 环 UI 闭合**。**诚实缺口**:真机 scaffold 需 kernel;OpenAPI section 是折叠 details 复用既有表单控件未单独截图。
- 2026-07-02 **govern 环硬化 + 巡检(「继续」)。** ⑬ **review-gate §14 produce 缺口修复**(`0206288`):`requires_review` 按 mutating-verb 子串分类,但 `produce` 不在表里 → `source_produce`(通用 §14 connector 写)**漏 review**(自托管 connector 写该像 vault.write 过审批,ADR-002 §14.9)。加 `produce` 进 MUTATING;`mcp_pack_publish` 本已被 `publish` 捕获;`source_describe/query` 保持只读不审。cargo 353 绿含 review_gate 分类测试更新。版本 →0.1.760。**巡检确认**:`OFFICIAL_PACKS` 硬编码**已退役**(Discover 全 `fetch_pack_registry` registry 驱动,§7.4「不做人工长尾列表」已落,无需再动)。
- 2026-07-02 **飞书定位纠正 —— 全仓扫平「飞书作 connector」错框(bao「与真相不一致的地方都要改,唯一真相」)。** 根:我把飞书当集成/挂载目标(挂官方 MCP),错 —— **CTRL 是本地/AI-native/数据主权版飞书**(原生 Smart-table=Bitable, Notes=docs),飞书是被替代的 incumbent,不 connect 你要替代的东西。全仓一次扫平:**ADR-006 §5 v9→v10**(删「CRM/ERP/飞书 examples are connectors」的飞书 + `ctrl-feishu`→`ctrl-twenty`;保留飞书作 competitor 对比 + IM-reach 合法轴)· `comms-system-design.md`(外部 connector 例去飞书)· `mcp-capability-marketplace.md`(飞书非 connector,降为 Bitable parity 参照 + 可选 sync-out mirror)· `capability-pack-map.md`(Tier-1 已在上轮纠;frontmatter 注释)· `feishu-mcp-research.md`(顶部 governing 纠正 banner + body retract)· brainstorm 一行。**保留(对的框)**:飞书作 competitor/incumbent 对比、Bitable parity(我们就是它)、IM-reach(触达客户在的平台,不同轴)、事实研究、历史 log(SSOT 已纠)。根因 + 防复发:memory `feedback-ctrl-is-feishu-not-integrate-feishu` + `feedback-connectors-built-by-irisy-not-dev`(已纠) + `feedback-jump-to-industry-default-not-ctrl-moat`。**真相链干净**:memory → ADR-006 → vault 设计文档全一致。
- 2026-07-02 **§14.13 统一写侧 —— 整套生产力套件收敛成一个 §14 系统(bao「你架构弄清楚了吗?是在建立整套系统吗?…好,做」+「全量做」)。** 根:读侧早已系统化(QuerySource + 共享 run_query),写侧碎成 ~10 bespoke smart_table_* 工具 = accretion 非建系统。**ADR-002 §14.13 v45** 锁镜像形态:`ProduceOp` 类型化 union(编译期 enum 反幻觉)+ `RecordSink` trait + 每源一个 `produce` 动词。四片全落(每片独立 checker):**S1** smart-table(`eed4e28`,checker 抓 full-regen schema 数据丢失 → in-place patch 保 markdown truth);**S2** task(`f0b82b5`,多 note 自持久化,checker 抓漏锁 → affected_notes 逐 note 锁);**S3** calendar(`80d19a4`,**第一个 trait-only 从零建的产品:3 动词 0 bespoke 工具**,一事件一 note 对齐 Obsidian Full Calendar;Sheets 裁决 OUT——与 smart-table 同为 record grid 不另开源);**S4** docs(`3eda019`,ProduceOp 显式扩展块级半边 Append/Replace/DeleteSection 按 ATX heading 寻址,supported_ops 双向证明)。跟进修复:S3 checker 抓 CJK 测试字面量(硬规则)+ 设计文档提前标 S4 ✅(做完前报完成,历史 commit 内已不可改,引以为戒)+ catalog 生成器 produce 误标 read + overflow 静默覆盖 → Err + fence 内 `#` 误判 heading(自查抓到,防 code block 边界撕文档)。**架构命题已验证:加产品 = 实现两 trait,零新 gate 工具**(91 tools / 393 测试全绿)。属 substrate 副产物线(§14 produce 契约 = ctrl-ghostfolio SC3 依赖)。剩:前端收敛到 produce + 退役 bespoke、UpsertRows update-by-key、CreateSource/AddView/DropView 变体。
- 2026-07-04 **ctrl-ghostfolio 端到端跑通 —— 模拟 Irisy 行为证成功能包命题(bao「开始 ctrl-ghostfolio 端到端,模拟 Irisy 行为」)。** P0-P3 substrate 早已落地(通用 §14 source / scaffold / validate / install / produce-review / publish / per-pack 投影),但**从没端到端串起来跑过一遍** —— 本 session 补上「模拟 Irisy 从创造到操作的完整行为」的活体证明(真实 Irisy 需 bao 机器 + LLM key,harness 是诚实替身)。**`scripts/debug/pack_e2e.py`**(dev-loop 绿 + 独立 checker PASS,commit `d9f56c3`)驱动整条链经真实 :17873 gate,10/10 断言全绿:
  - **create**(Irisy mcp-creator):`mcp_pack_scaffold`(OpenAPI→record_source 草稿)→ `mcp_pack_validate`(evals,质量护城河)→ `mcp_pack_install`(落 `~/.ctrl/mcps`)。
  - **uplift**(§14 AI-native 提升层):`source_describe`(6 §14 字段)/ `source_query`(allocation>10 实时抓取并剪掉 GLD=7.6,证明 query 真跑非镜像)/ `source_produce`(kind→type body-map 大写)。
  - **护城河**(数据主权):`source_produce` as **irisy** 静默过(user-surface 豁免)vs as **hermes** 被 review gate **拦住** → 后台批准后才记 —— live 证明 `is_user_surface` moat 覆盖 brain(不只是 UI 层)。
  - **share**(分享中心):`mcp_pack_publish`(evals-first → POST registry → ref)。
  - 配套:dev-only `/debug/secret/set` 端点(`debug_assertions`/`CTRL_DEBUG` gated + localhost + Bearer,注入 mock creds 代表真机 user-config/provision bootstrap)+ `test_all.py` BASE 支 `CTRL_GATE_BASE` env(headless kernel 跑 :17999 不扰 running app)。隔离用 id `ctrl-ghostfolio-e2e` 不碰真实 pack + 自清理。
  - **命题证成(活体)**:功能包三属性(§7.5 产品级 §14 uplift + Irisy 创造 + MIT commons 分享)ctrl-ghostfolio 全齐;create→govern→operate→share 整链通;加一个 REST connector 全程**零 per-connector 代码**(纯 manifest 数据 + 通用工具)。**诚实缺口(不变)**:真实 Ghostfolio 实例(docker `provision.service`)+ 真实 public registry 需 bao 机器,mock-HTTP 是同一既有替身。**下一步候选**:pack_e2e 进 CI smoke(nightly)· §7.5 v48 workspace(智能表格操作界面)前端 · 真机 Tauri app 点核验。
- 2026-07-04 **§7.5 v48 workspace 前端落地 —— 功能包的智能表格操作界面(dual-face,飞书 Bitable 式)(bao「§7.5 workspace 前端」)。** Explore 摸底发现渲染逻辑其实早已 shipped(FeaturePackScene 按 `table_prefix` 列 vault 表 → 每表一 Tab SmartTableViewer 多视图 + feature-pack.ts snake→camel map),真缺口 = manifest Zod schema 没 `workspace.table_prefix` 轴(SSOT 只在本地 mirror)+ ghostfolio 没声明 + 无视觉验证。落地(dev-loop 绿 + 独立 checker PASS,commit `08c68c8`+`798ec6b`):
  - **schema 轴**:manifest-schema.ts 的 `workspace` object 加 `table_prefix`(regex enforce `tables/<pack>-`),入 SSOT。
  - **dual-face(核心)**:有 §14 record_source **且** workspace 的 pack(ghostfolio)现在 dual —— connector 持仓(records)作第一 tab + 用户 vault 表作其余 tabs。之前 workspace 优先会**盖住持仓**(退步),现在 records 作第一 tab 不丢。复用 SourceDataView + SmartTableViewer(通用构建器)**零 per-pack 组件**;action error/output 也在 workspace face 显示(不静默失败)。
  - **ghostfolio manifest** 声明 workspace(`tables/ctrl-ghostfolio-`),种子 pack 有真操作界面。
  - **验证**:web tsc + vitest 191 + pack_e2e ALL GREEN(workspace 字段不破坏 validate/install/operate)+ **Playwright 视觉铁证** `/pack-lab`:tab 栏 [Ghostfolio(持仓)][analysis][watchlist],默认持仓 4 行,点 watchlist 渲染完整 8 视图表(NVDA/TSLA/VTI + Creator Panel),CF Workers(tools-only)无回归。独立 checker PASS(修了它挑的 action-feedback should-fix)。
  - **附**:修一个自己写 dual 时误入的 NUL 字节(join 分隔符 → 让 git 判文件 binary)+ 加 `.gitattributes`(源文件当 text,防控制字节翻 binary)。**§7.5 三属性全齐 ctrl-ghostfolio 种子完整**:产品级(§14 uplift + workspace 操作界面)+ Irisy 创造 + MIT commons 分享。**诚实缺口**:真机装包 click-through 需 Tauri app(browser `/pack-lab` mock-render 标准限制)。
- 2026-07-04 **功能包装/升级链两个 gap 补上 —— builtin 版本 re-seed + kernel→PWA pack 事件(自动出现+打开)(bao「用户如何装这个功能包」→「补上这两个 gap」)。** 真机核验 §7.5 workspace 时暴露:我一直用 gate `mcp_pack_install`(dev/brain 路径)装,绕过了用户真实路径(builtin 自动 seed / Discover 一键 `installPack`),所以装的没「自动出现+打开」。根因两个 gap(feat/pack-auto-install,commit `a82824c`,dev-loop 绿 + 独立 checker PASS):
  - **Gap 1 builtin 升级不生效**:seed 只在 `~/.ctrl/mcps/<id>/` 不存在时跑,app 升级带新 builtin(0.2.0)后已 seed 的 pack 停在旧版(0.1.0,无 workspace)。`builtin_mcps.rs` 加 `manifest_version`+`builtin_is_newer`,bundle 版本更新则 re-seed(pack 目录纯定义,用户数据在 vault+keychain §7 cap_asset,re-seed 安全)+ 2 单测。
  - **Gap 2 kernel 装 pack 前端不感知**:经 :17873 gate 装(Irisy/brain)或 seed 升级,运行的 PWA 不知道(`PACKS_CHANGED_EVENT` 是浏览器事件,只有 PWA 自己装才 dispatch)。kernel 现在装/卸后经 :17872 event bridge 推 `PacksChanged` op;前端 `initKernelPackEventListener` 订阅→re-dispatch `PACKS_CHANGED_EVENT`(reload)+ **auto-open** 新装的 pack(对齐 Discover 装的「出现+打开」)。`OpKind::PacksChanged` + `serve/router` 加 optional `EventWsBridge`(headless None no-op)+ `AmbientWorkbench` mount 订阅+auto-open effect。
  - **验证**:cargo test 434(含 2 版本单测)+ web tsc + vitest 191 + 独立 checker PASS + browser mount smoke(effect 在 :17872 不可达时静默重连不 crash)。**诚实缺口**:kernel→:17872→PWA→auto-open 整环需真机 Tauri app(:17872 token in-memory + `subscribe` 是 Tauri command,headless 够不到)= 既有 :17872 层 gap,待 bao 真机点核验。
- 2026-07-05 **ctrl-ghostfolio 完整 §14 uplift 真机 Docker 证成 + 用户可达性全链(bao「用户怎么装 ghostfolio」→「回 provision+bootstrap 主路」→「做 produce slice」)。** 从「用户搜不到/装不上 ghostfolio」一路做到完整 §14 describe/query/produce 在真机 Docker 上跑通。8 个 PR(#141-148),每片 dev-loop 三层验证 + 独立 checker PASS。
  - **可达性**(#144-145): 功能库(Discover)列 installed builtin + 可搜(之前只 registry remote,builtin 在主输入/功能库搜不到) + connect-existing config wizard(无 docker 填 URL+token,修 keychain vs credential_vault 双后端不对齐真 bug —— config wizard 存 keychain SERVICE `app.ctrl`,connector 只读 credential_vault,手动连静默失败) + not-configured 友好提示(替原始 -32602 JSON)。
  - **方向纠正(关键)**: bao 质疑「外部接入最佳方式,调研过了吗」→ 查 `feature-pack-provision-auth-engine.md`(bao 2026-07-01 亲定 governing)发现我把 **manual 手填(调研明排「最后兜底」)当主推**,偏离了调研定的最佳 **provision+bootstrap(零手填)**。命中 memory `feedback-jump-to-industry-default`。回主路。
  - **provision 主路真机验证**(#147-148, colima Docker): **完整 §14 三动词第一次真机跑通** —— provision(`docker compose up` 真 Ghostfolio+postgres+redis)→ bootstrap 自动铸 token → capture_context 拿默认 accountId → describe/query 读持仓 → produce 记交易(POST /api/v1/activities)→ query 显示(qty 10 value 1900)。零手填。
  - **4 个 mock 碰不到、真机才暴露的 bug 全修**: ①`docker compose` 插件缺失(引擎只探测 `docker version` 不够 → 探测真 compose + fallback 独立 docker-compose/podman)②`sslmode=prefer` 与 ghostfolio Prisma 7.8 崩溃(→disable)③produce 端点 `/api/v1/order` 被 ghostfolio latest 删→`/api/v1/activities`④produce 需动态 accountId。
  - **通用 write-context 机制**(非 ghostfolio hack): `BodyField.from_secret`(写体字段从 stored secret 拿,kernel-side 不进 LLM)+ provision `auth.capture_context`(token-exchange→GET/POST→JSON pointer 捕获→存 context id)。任何自托管 connector 的 workspace/tenant/account id 写操作声明式复用**零新代码**。Rust serde + TS Zod schema 同步 SSOT。
  - **命题深化**: 功能包不只 substrate 证成,而是「普通用户**找得到 + 用得上 + 完整 AI-native 读写操作**」真机跑通;接入双路 = **最佳(provision+bootstrap 零手填,需 docker)+ 兜底(connect-existing 填 URL+token,无 docker)**。**诚实缺口**: ①无 docker 引导装(`provision.tools` 设计了没接线,`tool_installer.rs` dead_code)—— 普通用户没 docker 仍卡在报错(下一 slice)②MANUAL 交易 query 的 symbol/name 空(ghostfolio 异步 recompute 显示细节,qty+value 对)。真机环境(colima + ghostfolio 栈)保留供 bao 真机看。
- 2026-07-07 **ctrl-stock-cn 功能包闭环达成(第二个种子,比 ghostfolio 更硬 —— Irisy 现写的服务,非连现成 app)。** 功能包真命题的活体闭环证成:
  - **三层齐 + gate 治理**:① 数据/工具层 = `stock-cn_*` 服务(market_mood / stock_kline / **stock_technical**(MyTT 全套衍生指标)/ quote / screen / ladder,多源 EODHD→Yahoo→EastMoney 全球可达)② skill/方法论层 = stock-analysis-cn(缠论结构优先)+ mood-cycle(92科比四阶段)+ daily-review,**全从真实可用产品蒸馏**(a-share-data MyTT / dalh 92科比 / chanlun-trading-system),非训练常识自造 ③ 智能表格层 = stocks-monitor/review/screen(vault 真表持续盯)④ `:17873` gate 审计/可见性。
  - **配置驱动的知识层(不硬编工具描述)**:manifest 声明 `knowledge_base` + `skills` → 三 skill 收进包 KB `projects/stock-cn/skills/` → 打开场景 runtime 注入 kb 指针 → Irisy **自动 skill_read/vault_read 方法论**。真机验证(账本铁证):有指针 → Irisy 读两 skill 走「市场 regime → 板块 → 缠论结构(中枢/背驰/买卖点)→ 指标当过滤器 → 决策带失效点」;无指针 → 退回指标堆砌。产出从「RSI 超卖→轻仓反弹」升级为有纪律的「观望 + 失效点先写」。
  - **关键 learnings**(bao 多轮校准,存 memory `reference-stock-methodology-research-corpus`):个股分析必须 regime-first(不孤立看单股)+ 结构优先(指标是过滤器不是买卖点)+ 方法论从 16 个 clone 的真实 skill **源码**蒸馏(不凭训练瞎编);数据只需交易实际数据(价量)+ 衍生指标(MyTT),不依赖付费基本面。
  - **诚实 gap**:换手率/资金流/龙虎榜/连板情绪等深度数据要国内端点(腾讯/东财),此机被本地 Clash fake-ip 墙 → 候选解:EODHD 付费 / 中国出口代理(AWS 东京中继)/ Clash 加 DIRECT 规则(全球可达的价量+指标层已全通)。
  - **意义**:create(Irisy 写服务)→ govern(gate)→ uplift(缠论 skill 让分析 AI-native)→ Irisy 自主端到端操作,四环齐。**下一步 = 证「distribute + 用户如何用」这最后一环**(见候选目标)。
